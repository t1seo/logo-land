# Logo Land — Mobile app icon artwork

## Summary

Research and adapt IP character generation, add six conversational app-icon styles, and preserve eleven independently generated originals. User steering adds an original Logo Land identity, stronger construction instructions for five non-IP styles with five fresh comparison samples, and concise bilingual documentation with individual sample pages. Research, implementation and sample generation are authorized together. Orca run: `run_1bb653ce3b14`.

Status: contract finalized on 2026-09-13 KST; implementation authorized. All three research/review workers completed their assigned checks and were released.

## Context and scope

The user requested research → plan → implementation → samples, referencing https://github.com/s1dashu/ip-as-logo-skill. Work starts on main at `ffecf1538963ddca5df1119d4e9d4d8dd2d5aac5` in the existing workspace. Preserve native Codex generation, original PNG bytes, exact prompts, immutable revisions, English default documentation and Korean translation.

This plan adds app-icon artwork. It does not repeat or erase the exhausted native requests in `logo-land-color-workflow.md`, change its unchecked acceptance gates, or publish its v0.4.0 draft. A creative icon gallery and a color-validated export are different operations with explicit behavior. A PNG does not establish an Icon Composer document, Android adaptive layers, an app build or store acceptance.

## Research and Metis consultation

- Research: `docs/research/app-icon-tools.md` and `docs/research/app-icon-sources.json`.
- Code/test exploration: `docs/qa/app-icons/code-map.md`.
- Metis review: `docs/qa/app-icons/metis.md`.
- Pinned upstream: `acb834c717bcd0a487c49732d08397ba280d690b`, MIT, Copyright (c) 2026 s1dashu.

## Contract

1. Add frozen, extra-forbidden `AppIconIntent` with `preset` = `ip_mascot|pictogram|abstract|monogram|soft_3d|pixel_art`, non-whitespace `subject` (1–500 characters), required `placement` = `center|lower_left|lower_right`, and `text: str | None = None`. Monogram text is required, 1–8 Unicode code points, without whitespace/control characters; other presets require null text. Preserve Unicode exactly. Placement is an explicit choice for every preset: conversation defaults IP to lower corners and other styles to center, but honors an explicit alternative. This deliberately treats upstream placement as a default, not a parser prohibition.
2. Add optional `app_icon` snapshots to Brief, Artifact, EffectiveIntent and PromptResult. Keep session v2 and manifest v2 for these additive optional fields; leave the frozen v1 parser unchanged. Older readers are not promised to read new icon-bearing v2 files. Existing v1/v2 reads preserve state bytes; first successful v1 mutation preserves the current byte-exact backup behavior. Existing artifact intent cannot be rebound.
3. A new icon brief requires opaque background, empty slogan and null lockup. Its exact_text is empty except for monograms, where it equals app_icon.text exactly. An explicit `--app-icon-file` on a legacy brand session declares an icon transformation and suppresses historical brand lettering/slogan/lockup in rendering while preserving history. Icon plus explicit lockup or explicit transparent import fails `intent_conflict` before mutation. Do not infer a monogram from the brand name.
4. Prompt/import precedence: explicit complete icon file; otherwise parent's app_icon including null when a parent exists; otherwise brief.app_icon. Parse both CLI inputs through the same boundary. Icon imports store requested_background=opaque when omitted; explicit transparent is rejected. Non-icon background omission retains the original brief behavior. PromptResult adds optional requested_background, populated opaque for icon mode; parent_requested_background remains historical and unchanged.
5. Preserve palette resolution and every existing strict color gate. Semantic IP colors do not create max_colors/allowed/locked constraints. Use existing free-text brief.palette and optional PaletteVersion roles for subject/background mapping. IP prompts name a solid background and two subject color families by default; a default fallback is warm yellow and deep navy on muted sage when no palette is supplied. User-defined colors or explicit strict palettes take precedence. No style implicitly relaxes a strict palette or changes existing thresholds.
6. Create a dedicated icon prompt branch. IP prompts describe only the square image and character, without logo/app-icon/use-case framing, historical text, generic centered safe-margin instructions or alpha/opaque/transparency vocabulary. Other presets get distinct shape/rendering directions; only monogram renders its exact text. Final trusted constraints follow quoted descriptive input. Check the complete icon prompt against the existing 20,000-character import limit before a native call. This is prompt hygiene, not model-level injection immunity.
7. Every icon is requested as one full-bleed square raster with square outer corners and an opaque complete background; actual returned dimensions and pixels are preserved. Request approximately 1536 square in prompt text, record actual output size and the runtime's actual tool/provider. Model identity is unreported if not exposed. No unsupported model/size/n/negative_prompt tool parameters, external API fallback, resizing or background replacement.
8. Add `icon-presets` JSON discovery and `icon-gallery --session ID --artifacts comma-separated-IDs --output relative-directory`. The gallery accepts explicitly selected icon artifacts regardless of selection/review/color/export status, verifies exact original hashes, and publishes exclusively without mutating state. Include every selected original, its exact prompt as a text file, intent, actual dimensions and a machine-readable manifest. Reject non-icon IDs, duplicates, empty selections, reserved/escaping/symlink paths and existing destinations; roll back owned staging on failure.
9. Gallery controls: square/rounded/circle CSS previews, 32/64/128px display sizes, light/dark surfaces and preset filtering/reset. Preserve images with contain behavior, label masks as illustrative, use accessible buttons and text-safe rendering, work from file URLs without network requests, and provide original download links. Never rank or gate creative samples on conformance, display default alpha/color PASS badges, or claim platform readiness. Existing color/sample galleries remain intact.
10. Optional approved export keeps every current selection, visual-review, background, integrity and fresh strict-color check; add selected app_icon metadata and a concise artwork/platform limitation to its manifest/guide. Keep the three existing ZIP payload filenames and original PNG bytes. Do not add a bypass flag or a second platform export engine.
11. Native sample IDs are fixed before calls: ip-a1, ip-a2, ip-b1, ip-b2, ip-c1, ip-c2, pictogram, abstract, monogram, soft-3d, pixel-art. Each uses its own helper session to avoid parallel revision races. IP subjects: owl (reading guidance), capybara (calm reading routine), puppy (friendly reading companion), left/right once each; other examples demonstrate weather, focus, notes with Korean lettering, plant care and a timer. Treat products as fictional examples. Each candidate is generated once without a reference to another candidate, imported using its exact prompt/intent/revision, and preserved without artistic filtering or auto-repair. A durable per-call receipt records planned/running/returned/failed/unknown state, prompt hash, IDs, tool/model provenance, returned paths/hashes and dimensions. Resume the same receipt; never use newest-file scanning or reset an unknown attempt.
12. Consolidate original samples through normal imports into a separate gallery-catalog session with truthful source receipts; no second native call is claimed for catalog copies. Publish docs/app-icons/index.html with all eleven originals, prompts and intent. Source/native/session/catalog/download hashes must agree. A real failed call cannot count as a generated sample. Any software issue is fixed with baseline/RED/GREEN verification; artistic variance is preserved and shown.

Metis's minimal optional model and separate prompt/sample branches are adopted. The explorer's generalized AssetIntent union, v3 migration framework and new icon-review schema are deferred: current scope needs additive intent and existing review/export semantics, with forward-only compatibility explicitly documented. Research's alternative style taxonomy remains research; the six IDs above are the implementation contract.

## Verification strategy

Use existing uv/pytest/Ruff/basedpyright infrastructure. Pin affected old behavior before a new failing test; then implement and exercise the actual CLI. Gallery QA uses Chrome and real file downloads; CSS previews never modify original images. Record all applicable adversarial cases and task-owned resource cleanup under `docs/qa/app-icons/`. Every worker has explicit file ownership, and every settled Orca worker is released or immediately reassigned.

## TODOs

- [x] R1 — Finish pinned upstream/platform research and decision-complete contract.
- [x] T1 — Implement typed icon intent, prompt/import binding and CLI with compatibility tests.
- [x] T2 — Implement portable icon comparison gallery and truthful delivery metadata.
- [x] T3 — Integrate conversational guidance, attribution, bilingual instructions and development version.
- [x] T4 — Generate and preserve eleven native samples using the implemented plugin.
- [x] B1 — Create an original Logo Land identity and apply it to both READMEs and plugin branding.
- [x] Q1 — Research and audit non-IP app icon quality, then finalize the improvement contract.
- [x] Q2 — Improve five non-IP prompt recipes with compatibility PINs, RED/GREEN and actual CLI QA.
- [x] Q3 — Integrate product-specific concept construction into conversational skill guidance.
- [x] Q4 — Generate and preserve five fresh same-subject native comparison samples.
- [x] Q5 — Publish the sixteen-original comparison gallery and verify real Chrome comparisons.
- [x] D1 — Research and simplify bilingual README content and linked individual sample pages.
- [x] T5 — Run integrated checks, real Chrome gallery QA and refreshed personal installation.

### Dependencies and ownership

| Task | Depends on | Owned files / responsibility |
|---|---|---|
| R1 | None | Research/code-map/Metis reports; coordinator owns this plan and ledger |
| T1 | R1 | New app_icon_models.py/app_icon_prompts.py/app_icon_presets.py/app_icon_cli.py; brief/artifact/models/intent/prompts/workflow/cli_options/logo_project.py integration; icon model/prompt/workflow/compatibility tests |
| T2 | T1 model contract | New app_icon_gallery.py and gallery template; delivery.py and new icon-guide module; icon gallery/delivery tests. T1 owns all CLI entrypoint/registration work; T2 exposes render_app_icon_gallery(store, state, artifact_ids, output_relative), returning the existing GalleryResult type |
| T3 | R1 contract | SKILL.md, app-icons.md/IP attribution reference and LICENSE, assets/app-icon.example.json, project-files/native-image guidance, README EN/KO, docs index/release guide, CHANGELOG, notices, plugin manifest, pyproject/lock; no helper source |
| T4 | T1 passing core | Native workers own one independent session/receipt each; coordinator owns sample manifest/catalog/aggregate gallery publishing after T2 |
| B1 | T1–T4 | New independent native brand logo, versioned PNG, current brand documentation, README EN/KO brand sections and plugin interface logo/color; preserve historical originals |
| Q1 | T1–T4 | Parallel primary-source quality research and independent five-style visual/code audit, followed by Metis and coordinator contract; `.omo/drafts/app-icon-quality.md` records user steering |
| Q2 | Q1 | Five returns in app_icon_prompts.py; test_app_icon_quality.py, intentional current monogram assertion in test_app_icon_prompts.py, new quality-fixtures and quality-core.md |
| Q3 | Q1; final CLI check after Q2 | Non-IP instructions in SKILL.md and references/app-icons.md; quality-guidance.md; preserve all IP guidance |
| Q4 | Q2, Q3 | Five independent per-ID source workspaces/native receipts; quality-sample-plan.json and quality-native-samples.json/md |
| Q5 | Q4 | New quality catalog and docs/app-icons-quality-v1; quality-comparison.md and quality-chrome evidence; sole sequential Chrome owner |
| D1 | Independent IA research; implementation waits for final new sample assets | Concise English/Korean README, docs navigation and individual sample pages; mandatory IP upstream adaptation credit; separate ownership from quality source/skill guidance |
| T5 | T1–T4, B1, Q2–Q5, D1 | Final integrated outputs and installed payload including new branding/guidance; no shared core edits during review |
| F1–F4 | T5 | Five independent Orca review reports: goal, code, hands-on QA, security, context |
| T6 | F1–F4 | Coordinator verifies, commits/pushes and records final state |

T2 and T3 execute in parallel once the contract exists; T2's runtime tests wait for T1's actual models. Native candidates run independently after T1, alongside gallery/docs completion. No worker changes another worker's files; failures return to the owning task.

### Acceptance and actual QA scenarios

**R1:** Upstream SHA/license and official platform sources are recorded. HTTP 200 bodies match pinned/local files in all three reports, source links resolve, and task resources are cleaned. Commit with the feature research docs.

**T1:** Run the relevant existing background/palette/transaction/CLI tests as baseline before source edits. Add failing tests for all six presets, Unicode monogram, malformed/extra/null fields, explicit and inherited icon transforms, legacy-parent null, immutable intent, v1 exact backup, byte-preserving reads, stale revisions, strict-palette preservation and icon/background/lockup conflicts. Actual channel: in an owned tmux session use `tmux send-keys -t ll-icons-core 'uv run --locked python skills/logo-land/scripts/logo_project.py icon-presets' Enter` then `tmux capture-pane -pt ll-icons-core -S -200`; PASS is exit 0 with six exact presets. Also execute real init → prompt → import → show with a clearly labeled synthetic PNG and a malformed icon file that exits nonzero without mutation. Preserve receipts in docs/qa/app-icons/core.md and terminate the owned tmux/session fixtures. Commit: `feat: add app icon intent and native prompt workflows`.

**T2:** Baseline existing color-gallery/export behavior, then RED tests for original availability without review, text-safe HTML, byte-equal images/prompts, selection order, same-artifact metadata, immutable state, empty/duplicate/non-icon selections, reserved/escaping paths, destination collisions, tampered hashes and rollback. Actual channel: serve the generated fixture gallery only on task port 8768, `curl -i --fail http://127.0.0.1:8768/index.html`; PASS is HTTP200, valid controls and exact original paths, not artistic compliance. Record gallery.md and delivery.md with server PID/port teardown. Integrated Chrome testing follows under T5. Commit: `feat: add app icon previews and artwork metadata`.

**T3:** Parse the example with the actual model, exercise documented icon-presets/init/prompt commands, verify local links and MIT notice bytes, and validate aligned 0.5.0 versions/uv lock. Actual channel: task tmux ll-icons-docs runs the documented prompt command, captured output must expose the matching resolved icon and opaque request without a native-call claim. Record docs.md; remove only owned fixtures/session. English remains default, Korean stays complete, release badges remain truthful. Commit: `docs: add app icon workflows and attributed IP guidance`.

**T4:** Invoke the real native tool once per candidate using helper-built final prompts; each worker saves exact tool receipt and original before normal import. Actual channel is the real native tool, plus T5 Chrome showing all returned images at all preview sizes. PASS requires 11 actual independent original files and traceable prompt/intent/import receipts; artistic preference, incidental shading or imperfect output is not a rejection criterion. Record native-samples.json/md with every attempt and unchanged source hashes. No task server required during generation; persistent project originals are deliverables, not temporary cleanup targets. Commit samples with the final documentation.

**B1 (user steering, 2026-09-13 KST):** The user requested a new independent Logo Land logo instead of the previous parody, applied to the README. Create one suitable master concept through the existing brand-logo skill and native image tool without using the prior logo as a reference. Use an original design about turning ideas into marks, legible exact LOGO LAND lettering, and a restrained ivory/charcoal identity with one optional accent. Save the new PNG at a versioned path and preserve original pixels, prompt, intent, provenance and review. Update both READMEs and plugin interface branding; keep historical parody originals and evidence as explicitly historical documents. Replace the old branded transparent demonstration in both READMEs with an existing verified generic transparent sample, without claiming the new opaque master is transparent. Actual QA: view the new original, render README branding at representative sizes in a task-owned HTML preview, check HTTP 200 and exact PNG bytes, validate both README links/manifest and archive hashes. If no software changes are needed, do not manufacture source tests. Refresh the personal payload after branding stabilizes. Existing icon gallery and its native receipts remain byte-identical.

**Q1 (user steering, 2026-09-13 KST):** The user finds the IP style stronger than the other five and requests research and quality improvements. In parallel, examine primary app-icon design guidance and public skills, and visually audit all five actual non-IP originals with their exact prompts and 32/64/128px Chrome evidence. Preserve the IP recipe and every historical original. Record concrete weaknesses, style-specific proposed improvements, relevant code/test boundaries and same-subject comparison criteria. Review with Metis, then add decision-complete implementation, new-sample and verification tasks to this same plan before changing generator behavior. Treat product-readiness comments as creative feedback, not store approval. Source/installation/final review evidence must be refreshed after the eventual quality changes. Research and audit evidence: `docs/research/app-icon-quality.md`, `docs/research/app-icon-quality-sources.json`, `docs/qa/app-icons/quality-audit.md`; actual bounded HTTP 200 primary-source/original checks and registered resource cleanup are required.

**T5:** `uv run --locked pytest -q`, `uv run --locked ruff check .`, `uv run --locked basedpyright`, `uv run --locked ruff format --check .`, `uv lock --check`, manifest validation and local-link checks. In actual Chrome open `file:///Users/cillian/Documents/Github/Projects/logo-generator/docs/app-icons/index.html`; exercise square/rounded/circle, 32/64/128, light/dark, each preset filter, reset, keyboard navigation and original/prompt links. Download at least one IP and one monogram original and compare hashes. Capture original screenshots and actual observations, without classifying IP artwork as approved/rejected. Refresh the personal plugin through official cachebuster/reinstall flow, verify source/cache payloads, and run installed helper icon-presets/init/prompt/import/gallery plus a non-icon regression from a separate workspace. After B1, refresh changed branding files and recheck payload/manifest identity; reuse runtime checks when source is unchanged. No new image call is needed for installation verification. Record integrated checks, chrome.md and installation.md; stop owned servers, remove downloaded test copies and close task-only automation contexts while leaving the requested persistent gallery tab available.

All applicable adversarial classes must be observed or explicitly ruled out: malformed input, prompt injection, cancel/resume, stale state, dirty worktree, hung commands, flaky tests, misleading success and repeated interruptions. Use task-owned fixtures for interruption/rollback checks; never kill a user process or alter saved samples to manufacture evidence.

**D1 (user steering, 2026-09-13 KST):** Research official README guidance and pertinent primary plugin examples, inventory existing samples, then finalize a concise user-facing information architecture before rewriting both READMEs. Center the existing badges and language links; keep the new original hero, clear purpose, installation, practical usage, compact capabilities and sample navigation. Give each showcased sample a linked individual page, reusing useful existing pages where possible. Remove research/production rationale and diagram-tool narration from the primary README; retain necessary attribution and explicitly state that the IP character workflow references/adapts `s1dashu/ip-as-logo-skill`. Preserve legal notices and deeper evidence, original pixels, old links and truthful capability limits. Coordinate new quality samples before publishing final pages. Actual QA requires links/image/download byte checks, HTTP200, Chrome navigation and representative responsive layouts; document ownership, all applicable adversarial classes and cleanup. Research: `docs/research/readme-structure.md`; architecture/QA: `docs/qa/app-icons/readme-ia.md`. Final path map and page acceptance are added after research.

## Finalized quality extension contract

The primary [quality research](../docs/research/app-icon-quality.md), [visual/code audit](../docs/qa/app-icons/quality-audit.md) and [Metis review](../docs/qa/app-icons/quality-metis.md) are adopted with the choices below. The new README architecture is separately owned by D1; it does not delay the Q2/Q3 source and skill work. Public research/tool narration will not be added to the simplified README.

1. Change only the five non-IP return blocks in `style_direction` inside `skills/logo-land/scripts/logo_helper/app_icon_prompts.py`. Preserve the function signature, exhaustive matching, entire IP block, `placement_direction` and shared builder exactly. No public model, CLI, palette, import, export, history, gallery or provider change. Defaults yield to explicit subject/concept/changes and the authoritative lettering/placement/palette tail. No universal fill percentage, hard grid validator, aesthetic score, font installation or new approval.
2. Pictogram: one compact flat emblem, distinctive contour, substantial filled shapes, consistent curves/corners, deliberate overlap and open identifying gaps. Abstract: one coherent nonliteral gesture with shared curvature/weight, intentional ends/intersections/openings and legible negative space. Monogram: exact code points and script topology, optical stroke/counter/join/internal-space balance, coherent terminals and confident scale; inter-glyph spacing only where multiple visible glyphs exist, without code-point-count branching. Soft 3D: one simple object with a strong silhouette, explicitly chosen material or smooth matte/satin default, near-frontal broad light, controlled highlights and uniform background; depth stays within palette constraints, incompatible extra tones and external shadows omitted. Pixel art: one coarse square module, repeated steps, consistent thickness, broad connected clusters and visible gaps; no mixed scales, blur, smooth gradients, dithering or stray blocks by default. Do not impose a literal 24×24 output/grid promise.
3. Q3 adds one concise concept step using existing fields: product benefit, named motif, one distinctive relationship/construction and the feature to preserve when small. Keep all IP conversation/count/default/attribution text unchanged. Infer supplied context; ask only consequential missing input. Do not hardcode a sample product into trusted style text. Generic style constraints stay trusted; dynamic product meaning stays in quoted `concept`.
4. Q2 must PIN full current IP output across three placements × fallback/free-text/strict palettes × generation/parent edit before source changes; retain exact pre-icon generation/edit and existing parent/null/history/strict-export tests. RED tests assert the reviewed emitted prompt contract, parsed descriptive data, exact Unicode (`모`, `메모`, decomposed `e` + combining accent), trusted-tail order and style isolation, including two different concepts. Test complete prompt length with the added instructions and preserve effective parent/palette/revision metadata. These are output-contract tests, never aesthetic tests. No policy abstraction solely for RED.
5. Keep `docs/qa/app-icons/core-fixtures/prompt-icon-monogram.txt` byte-identical as historical data. Add current `quality-fixtures/prompt-icon-monogram-v1.txt`, deliberately retarget only the current monogram snapshot assertion and verify old prompt import/gallery byte preservation. Preserve the IP assertion; never generate golden expectations by invoking the modified builder inside a test. New expected instructions are fixed before production changes.
6. Copy the complete five original briefs from `docs/qa/app-icons/sample-plan.json` unchanged. Only construction concept and five style recipes change. Use empty changes, no parent/reference image, actual approximately-1536-square request and preserve actual dimensions. Fixed new IDs are `pictogram-quality-v1`, `abstract-quality-v1`, `monogram-quality-v1`, `soft-3d-quality-v1`, `pixel-art-quality-v1`; source session/artifact uses the same ID. Each workspace is `output/app-icons-native/<id>/`, receipt `docs/qa/app-icons/native/<id>.json`, attempt `<id>-draw-1`. Register before calls, exactly one independent call per ID, every result preserved, no automatic artistic rerolls or pixel edits. Unknown/failed calls cannot be counted as returned and keep their identities.
7. Fixed concepts are the exact five concept cells in the Metis review's **Five fixed same-subject cases** table. Resolve the earlier alternatives explicitly: pictogram has one substantial sun crescent with no detached rays; soft leaf has no external cast/contact shadow; hourglass has one connected upper sand mass and an empty lower chamber. All original product/subject/palette/placement/text values remain unchanged. Record these in `quality-sample-plan.json` before dispatch.
8. Use existing helper normal imports in catalog workspace `output/app-icons-quality-catalog-v1/`, session `icon-quality-v1`, revisions 0–15, then `icon-gallery --output comparison`. Copy that complete generated directory exclusively to new `docs/app-icons-quality-v1`, checking every byte. Preserve existing `docs/app-icons` and all old prompts exactly. Order: six IP originals in original order, followed by each non-IP old/new pair in pictogram, abstract, monogram, soft-3d, pixel-art order. Catalog copies are not new native calls. New documentation links to this gallery and individual pages; no gallery runtime/schema change.
9. Q5 Chrome owner compares all pairs at 32/64/128 with identical settings, all masks/surfaces/filters/reset/keyboard, original/prompt links and exact downloaded hashes for unchanged IP plus each new result. Observe old/new at native size too. Record **improved / mixed / unchanged / worse** per style with concrete strengths and remaining defects. Preserve unfavorable outputs. A paired case study cannot establish a model benchmark or guarantee production/store readiness; artistic assessment does not bypass export/color gates.

**Q2 acceptance and channel:** Run the Metis baseline selection, new PINs and real RED before edits, then affected tests/Ruff/format/basedpyright and actual helper init → prompt for five styles/IP, strict soft-3D palette and an inherited edit. `tmux send-keys -t ll-icon-quality-core 'uv run --locked python skills/logo-land/scripts/logo_project.py icon-presets' Enter` and `tmux capture-pane -pt ll-icon-quality-core -S -200` must show exit0 and the six unchanged IDs. Capture all actual prompt JSON/expected metadata in `quality-core.md`. Register and remove exact `/tmp/ll-icon-quality-core` and owned tmux. Commit with feature after final reviews.

**Q3 acceptance and channel:** Check IP section/history hashes and every guidance link; execute documented init/prompt using meaningful product-specific concept through the real helper. Serve the final reference on owned8794 and run `curl -i --fail --silent --show-error --connect-timeout 2 --max-time 10 http://127.0.0.1:8794/app-icons.md -o /tmp/ll-icon-quality-guidance/guidance.http`; require200 and exact final bytes. Record `quality-guidance.md`; stop owned server and remove temp. No artificial source tests for Markdown changes.

**Q4 acceptance and channel:** Five independent Orca workers use the actual native tool only after Q2/Q3 pass. Save exact prompt/result/intent/revision and original before normal import; inspect actual returned image without filtering. Each task serves its original from its own workspace on ports8796–8800 respectively and uses `curl -i --fail --silent --show-error --connect-timeout 2 --max-time 10 http://127.0.0.1:<port>/original.png` to verify200 and original hash. Record per-ID receipt, malformed/stale/resume/injection/integrity probes in isolated fixtures, and exact PID/port/temp teardown. Persistent source workspaces are deliverables. Aggregate only actual returned originals.

**Q5 acceptance and channel:** Catalog source/native/import/published original and exact prompt hashes all agree for16entries; original11gallery remains byte-identical. Owned HTTP8801 verifies index, manifest and32downloads. Actual Sky Chrome at `file:///Users/cillian/Documents/Github/Projects/logo-generator/docs/app-icons-quality-v1/index.html` performs the cases above and saves raw privacy-clean screenshots; no image post-processing. Stop server, remove test downloads/temp, close owned QA tabs and leave the requested persistent gallery. D1 owns README/sample-page edits; catalog worker does not edit those files. Final integration/installation/five reviews wait for Q5 and D1.

All Q2–Q5 workers apply the existing nine-class QA and cleanup contract. Research-informed instructions and better concept construction are implementation outcomes; native visual conclusions remain separately observed. The final public version stays 0.5.0 development, public v0.3.1 and old unpublished v0.4.0 remain unchanged.

## D1 documentation contract

Adopt `docs/research/readme-structure.md` for concise entry content and GitHub-renderable individual Markdown pages. This is the final editorial decision, not a new generation or hosting feature. Preserve every existing PNG, saved prompt, HTML gallery engine, color/history receipt and legal notice. Keep English default and a complete `.ko.md` counterpart for each new page; no duplicated image files.

- Root README order: centered existing badges/language/docs links and current 320px hero; one-sentence purpose; short repository installation plus link to full plugin installation; three practical requests; four compact capability bullets and concise raster/platform limits; five sample-category/identity links; visible IP adaptation credit and useful support/changelog links. Target roughly80–110lines per language. Remove inline sample grids, research/QA tables, model/tool receipts, diagram and diagram-tool narration. Preserve former section fragments using explicit anchors near the corresponding new compact sections.
- Explicit credit in both READMEs and all six IP pages: IP character guidance is adapted from `https://github.com/s1dashu/ip-as-logo-skill`, with links to existing pinned adaptation reference, MIT notice and THIRD_PARTY_NOTICES. Do not declare the whole repository MIT or link a nonexistent root LICENSE. Keep other mandatory notices in place.
- Primary navigation uses `.md` pages that render on GitHub. Existing interactive HTML galleries remain optional local views; no deployment/GitHub Pages promise. New pages contain a title/language switch/back links, one concise description, actual image(s), one example user request, actual PNG/available package downloads and only a relevant result limitation. Label paraphrased requests as examples, preserve exact saved prompt files. Pair pages have separately labeled original/new images.
- D1-A owns new bilingual `docs/samples/items/<existing10id>/README*`, `docs/colors/projects/<existing7project>/README*`, `docs/samples/README*`, `docs/colors/README*`, `docs/samples/transparency*.md`, and simplification/new Korean counterpart of `docs/brand/README*`. Use the exact existing ID/project/image/package mappings in the research report. Separate old NORTHLINE NL from the later NORTHLINE name sample. GROVE original and warmer edit share one page with distinct anchors; only actual existing deliveries get package links. Failed/indeterminate color cases keep one factual limitation, never a fabricated approved/transparent result.
- D1-B owns new bilingual `docs/app-icons/README*` and `docs/app-icons/samples/<11existingid>*.md`, after Q5 catalog assets exist. Six IP candidates each get a page and an example requesting one specified subject/placement. Five non-IP product pages each show old and `*-quality-v1` original, actual filenames from the new manifest and exact `모` for monogram. No inferred aesthetic verdict; use observed Q5 conclusions if present, otherwise factual style description. Original11gallery stays byte-identical.
- D1-C owns root README EN/KO, `docs/README*`, `docs/installation*.md`, and `docs/archive.md` after D1-A/B pages exist. Archive the prior docs index at the same depth before simplifying it. Preserve installed-vs-direct repository usage distinction and truthful0.5development/public0.3.1 badges. Link named sample pages through compact category indexes. Do not duplicate the research narrative in primary navigation. Root owns final QA index/ledger/CHANGELOG; no worker edits another owner's files.
- D1-A can run beside Q2/Q3/native work. D1-B starts when the Q5 catalog owner declares its published assets stable; D1-C waits for A/B pages. One final Chrome worker can start Q5 comparisons against that stable gallery while D1 pages are authored, then checks D1 navigation and representative rendering only after those pages settle. This keeps a single GUI owner and makes the named page dependency apply only to page checks. Source integration can run independently after Q2/Q3 freeze; final T5 checks reverify hashes after documentation settles.

Each docs worker records a pre-edit link/image/anchor baseline, validates all final local paths, EN/KO parity, actual downloads/hashes and preserved originals, with no artificial production tests for Markdown. Actual HTTP channels use dedicated ports8802/8803/8804 for A/B/C and `curl -i --fail --silent --show-error --connect-timeout 2 --max-time 10` against a named final page, plus download verification. Reports are `docs/qa/app-icons/readme-pages.md`, `readme-icons.md`, `readme-rewrite.md`; exact temporary roots `/tmp/ll-readme-pages`, `/tmp/ll-readme-icons`, `/tmp/ll-readme-rewrite` are registered and removed with each owned server. Final Chrome checks actual linked PNGs/local galleries and a representative rendered Markdown preview, clearly distinguishing local preview from hosted GitHub rendering; screenshots and cleanup in `docs/qa/app-icons/readme-chrome.md`. Recheck old anchors, keyboard navigation and narrow layout, with all nine QA classes observed or specifically ruled out.

## Final Verification Wave

- [x] F1 — Independently verify the new icon goal and constraints.
- [x] F2 — Independently review code quality.
- [x] F3 — Independently exercise actual CLI and gallery behavior.
- [x] F4 — Independently review security and source/context fidelity.
- [x] T6 — Commit and push verified development work, record final state and cleanup.

Each review reads the full relevant diff against the stated base, exact plan, tests and sample receipts. F1 checks requested coverage and legacy preservation; F2 reviews type boundaries, module responsibility/size and meaningful tests; F3 exercises at least 20 concrete CLI/gallery scenarios; F4 uses separate security and context workers. Reviewers perform actual bounded HTTP/CLI/browser probes appropriate to their domain, retain an artifact and cleanup receipt, and distinguish executed evidence from static reasoning. Any blocking finding returns to its named owner for a regression test and correction, then independent recheck; completion requires all five perspectives to pass the new icon feature scope. Historical color release gates remain outside that verdict.

T6 acceptance: authored-file whitespace/local links pass, current helper and installation evidence correspond to final source, all worker resources are accounted for, commit and push succeed, local HEAD equals origin/main and the working tree is clean. Read the existing v0.4.0 draft and published v0.3.1 without publishing or retargeting them. Record exact final commit and remaining historical limitations; complete only this plan.

## Commit and release policy

Use the existing authorization to commit and push reviewed work to main. Increment development source to 0.5.0 for the new feature, align manifest/project/lock and personal cache, and identify it as unreleased. Keep published v0.3.1 and the prior v0.4.0 draft untouched. Public release publication is not part of this icon-artwork task.
