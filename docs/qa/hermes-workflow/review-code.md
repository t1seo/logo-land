# Logopia 0.8.0 independent code review

**PASS — high confidence. No open critical, major, minor or nit findings in the reviewed scope.** The five original independent findings and the later major reaping-race defect have fresh, unchanged or strengthened passing reproductions. Root's final [957-test integration run](final-integration/final-pytest.txt) passed; the installed plugin's 61 managed hashes and 60 repository payload hashes independently match. The initial **2 failed / 954 passed** integration result and all earlier RED evidence are retained below. No production source was changed by this reviewer, and all owned temporary resources are removed.

## Original findings (resolved in the first code-ready rerun)

1. **Major: an interrupted installation can delete the prior plugin.** In `integrations/hermes/logopia_studio/installer.py:141`, publication rollback catches only `OSError`. After the old install is renamed into the temporary backup, a `KeyboardInterrupt` during the second rename bypasses restoration; temporary-directory cleanup removes that backup. The real filesystem fixture ended with `target_exists=false` and an empty plugins directory. The identical boundary with `PermissionError` preserved the prior bytes, isolating the exception handling as the cause. [First failing test and passing control](review-code/install-rollback-tests.txt), [exact initial probe](review-code/install-rollback-probe.py.txt). The fix owner must preserve the previous install across interruption before reporting installation settlement.
2. **Major: launcher timeout settlement can leave its owned descendant running.** In `launcher_process.py:85`, `_settle` returns when the direct leader exits after SIGINT; it does not establish that the process group has finished. `_signal_owned` also stops signaling after `process.poll()` observes the leader's exit. A bounded same-group child that deferred SIGINT survived the returned timeout receipt as PID 19778, PPID 1, PGID 19775, state `S` (not a zombie). This weakens the guarantee that the launcher has settled before workflow interruption/recovery or profile unlock. [First failure](review-code/defensive-tests.txt), [probe](review-code/defensive-probes.py.txt). Cleanup must cover remaining owned descendants without signaling unrelated processes or claiming remote provider cancellation.
3. **Major: interrupting a helper invocation leaves its new-session process alive.** In `helper_process.py:15`, only `TimeoutExpired` enters termination/reaping. A real SIGINT to the invoking local test process propagated `KeyboardInterrupt` while helper PID 19859 remained `Ss`; Python also emitted a resource warning. Since helper processes start a separate session, the outer Hermes group's signal is not a substitute for helper cleanup. This is a potential continuing writer after interruption, not just a log issue. [First failure and OS state](review-code/defensive-tests.txt). Every local interruption path must settle the helper before propagating it.
4. **Major: a nested foreign marker file is deleted by managed update.** `installer.py:79` exempts `path.name == '.logopia-install.json'` at every depth, although only the root marker is owned metadata. An ordinary foreign file was refused and preserved; identical fixture-user content at `user-notes/.logopia-install.json` passed update validation and disappeared when the old directory was removed. [First failure/control observables](review-code/defensive-tests.txt). The exemption must identify the root marker exactly and preserve all other foreign files.

The process probes used only finite local Python fixtures. Each surviving PID was explicitly terminated and confirmed absent before its assertion failed. The installer probes used only owned scratch profiles, never the installed Hermes profile. The four findings were routed to root; no failing assertion was relaxed or deleted.

## Scope, requirements and source binding

Reviewed against `f7dd3268313d2422b87efea63eef8257b0e651b9`, including the untracked implementation, rather than relying on git diff. Read the complete 50 production native Python files and both production JavaScript modules, their template/director resources, and adjacent helper CLI, models, storage, PNG inspection, import/review/select/export, color-delivery and bundle boundary files. No directory-scoped AGENTS.md was found in the repository or applicable ancestors; the supplied polite-Korean instruction was followed.

Before probes, pinned 157 source/test/helper files, 13 protected files and 14 public sample files. [Source hashes](review-code/source-before.sha256), [protected hashes](review-code/protected-before.sha256), [public sample hashes](review-code/public-before.sha256), [initial dirty status](review-code/status-before.txt), [tracked diff scope](review-code/tracked-diff-stat.txt). The three original affected production modules are also retained as [pre-fix snapshots](review-code/pre-fix-snapshot.sha256).

| Requirement | Review assessment and evidence |
| --- | --- |
| G1: Python 3.11 native / locked 3.12 helper | Actual Hermes Python 3.11.16 parsed all 50 original and all 52 final native files without import execution. No `Any`, `object` or `cast` AST names, no `logo_helper` imports, maximum 229 nonblank/noncomment lines. The explicit helper argv uses `uv run --locked --project ... python ...`. [Original parser](review-code/source-audit.json), [final parser](review-code/source-audit-final.json). The lock diff changes only the helper's own version; dependencies are unchanged. |
| G2/G7: short locks, CAS, budgets, unknown jobs and superseded critiques | Reservations are saved under nonblocking workflow lock before host calls, and commits compare revisions. Request identity, successful attempts and originals cannot be rewritten. Explicit critique recovery links the exact unknown request and consumes the remaining attempt; late results cannot overwrite new review/selection. Five late-critique cases freshly passed. Process settlement has the findings above. |
| G3: helper two-store recovery | Deterministic candidate/session/prompt/parent/hash and export destination precede helper mutations. Fresh real helper tests recovered lost import/export replies without a second image/package and rejected foreign output/missing originals. These are recoverable two-store operations, not an atomic cross-file transaction. |
| G4: actual image evidence and conservative delivery | Host computes image/parent/target-view hashes and distinct call IDs; core checks both roles/five keys. Only permitted N/A cases pass; the helper still checks actual PNG/alpha/color facts. A fresh transparency mismatch test blocked delivery despite passing fixture critics. Tests are not evidence of aesthetic superiority. |
| G5/G6: saved intent, bounded inert data, exact feedback | Strict frozen models reject extras; request bytes cap at 32 KiB; notes/fields are bounded; revision/candidate digest are checked before mutation. Prompt/query data is serialized, argv is literal, and gallery draft selection is separate from canonical choice. Advisory colors remain explicit. Source reviewed and prior fixture evidence attributed below. |
| Gallery publication and delivery | Full Python/JS review found fresh exclusive output, escaped strings/inert JSON, no remote calls, exact original hashes, bounded manifest/guide/ZIP reads, matching ZIP members and selected-candidate binding. Publication rollback checks owned inode/bytes before unlinking; original downloads do not imply review approval. Browser evidence is attributed, not rerun by this reviewer. |
| Installation | Managed source hashes/settings and foreign/edited-file refusal exist. Both originally observed gaps pass unchanged final reproductions; the adjacent first-rename/failed-restore gap is covered by attributed independent and owner evidence. Unresolved backup bytes are retained, with the path attached to the exception. Default-profile or credential edits are not used by this review. |
| Claims and release truth | Native critics are distinct model calls, not humans; no legal/font/vector or measured aesthetic-superiority promise. IP reference credit and license accompany the director. v0.8.0 publication remains root-owned; a pending publication and the truthful historical v0.4.0 draft are not defects. |

## Fresh executions and exact commands

Assigned unchanged baseline, exactly:

```sh
uv run --locked pytest -q tests/hermes/test_core_critique_late.py tests/hermes/test_helper_recovery.py --basetemp /tmp/logopia-review-code-080/pytest
```

**10 passed in 25.38s**, exit 0; outer 180-second deadline not reached, elapsed 25.655s. [Output](review-code/assigned-tests.txt), [receipt](review-code/assigned-receipt.json), [finite runner](review-code/test-runner.py.txt).

Narrow justified defensive probes:

```sh
uv run --locked pytest -q -s -c /Users/cillian/Documents/Github/Projects/logo-generator/pyproject.toml /tmp/logopia-review-code-080/test_review_boundaries.py --basetemp /tmp/logopia-review-code-080/probe-pytest
PYTHONPATH=integrations/hermes uv run --locked python /tmp/logopia-review-code-080/test_install_recovery.py
/Users/cillian/.hermes/hermes-agent/venv/bin/python /tmp/logopia-review-code-080/source_audit.py
```

The first command had a 60-second outer limit; all children had a 10-second self-deadline, launcher 2 seconds and helper transport 5 seconds. **3 failed in 2.63s**, exit 1, not timed out: all failed the intended behavioral assertion. [Receipt](review-code/defensive-receipt.json). The installer follow-up had a 20-second self-deadline, **1 failed/1 passed in 0.05s**, exit 1. [Receipt](review-code/install-rollback-receipt.txt).

The required real manual channel was personally executed:

```sh
tmux new-session -d -s logopia-review-code-080 -c /Users/cillian/Documents/Github/Projects/logo-generator -x 180 -y 32 '/bin/zsh -f'
tmux send-keys -t logopia-review-code-080 'uv run --locked python integrations/hermes/studio.py show --workflow offcut-hermes-demo --json' Enter
tmux capture-pane -p -S -200 -t logopia-review-code-080
tmux send-keys -t logopia-review-code-080 'uv run --locked python integrations/hermes/studio.py show --workflow ../review-code-inert --json' Enter
tmux capture-pane -p -S -200 -t logopia-review-code-080
tmux kill-session -t logopia-review-code-080
```

**PASS:** actual success JSON reported r47/delivered/e2, five candidates, `logs:null`, exit 0. The malformed ID returned `invalid_id`, `success:false`, exit 1. [Before](review-code/tmux-before.txt), [show](review-code/tmux-show.txt), [malformed/after](review-code/tmux-malformed.txt). Canonical SHA was unchanged: `16eea6dc8c32e979c432fbdffee137d76cb68a230aad9e5b65cc740d59e26e04`. [Hash receipt](review-code/canonical-after-tmux.sha256). The owned session was closed; no model process was started.

## All nine adversarial classes (final observations)

| Class | Result and concrete observable |
| --- | --- |
| Malformed input | Fresh PASS on all three manual checkpoints: exact tmux malformed identifier failed with `invalid_id`/exit 1; no canonical change. [Latest channel](review-code/tmux-settled-unwrapped.txt). Prior strict request/schema/count/byte-limit tests are additionally attributed to the 907-test run. |
| Instruction-like brief/feedback as inert data | Attributed PASS: read `test_launcher_request_models.py::test_instruction_like_notes_are_literal_query_data` and `test_core_resume_adversarial.py::test_retry_preserves_choice_feedback_and_allows_edit_delivery`; those tests check exact received query/argv, preserved keep/change and absent marker files. Full-suite result is prior evidence. No adversarial live-model request was made or claimed. |
| Cancel/resume | Fresh PASS for five late/superseded critique variants, including cancellation after a newer choice and an edited child. Original snapshot and PNG hashes stay equal. Original helper-interrupt RED is resolved again in the [latest frozen-source test](review-code/settled-tests.txt), with no child or ResourceWarning at return. |
| Stale state | Fresh late-return cases keep the newly chosen revision exactly unchanged and old-job reconcile is a no-op. Explicit stale revision/hash rejection is additionally supported by reviewed existing tests and the attributed full-suite run. |
| Dirty worktree | Fresh PASS for foreign export sentinel, ordinary installer-file control and final nested-marker refusal/preservation. Existing dirty repo, untracked implementation and four unrelated draft hashes were pinned and preserved. Only coordinator-announced source/doc deltas are rebound. |
| Hung/long commands | Fresh final PASS: descendant PID 6089, interrupted helper 6251, nested helper 6259 and repeated-interruption helpers 6432/6442 were absent before returned-result assertions. [Latest six-case result](review-code/settled-tests.txt). Force-bound denial, numeric-inspection failure and separate-session control are attributed to owner boundary/scoped executions. The same defect's PID-query wait/poll/kill path was read and is protected; no remote hanging provider was created. |
| Flaky tests | Root's intermittent full-suite failure was retained, isolated with a deterministic real-signal toggle on both runtimes, and fixed in source. This was not an unchanged-source retry-to-green or weakened assertion. The original and deterministic variants now freshly pass together. Native stochastic aesthetic repeatability is N/A because no inference is authorized. |
| Misleading success | Fresh PASS: helper success without its original was rejected; fully passing fixture critics could not bypass background checks; final installer rejects/preserves the nested foreign file. Launcher exit/prose is separately checked against saved-state postconditions, and inspection failure is not a successful receipt. |
| Repeated interruptions | Fresh PASS for both original and acquisition-interrupt variants: original exception identity, reaping, no warnings and finite elapsed bounds hold. [Result](review-code/settled-tests.txt). Owner's actual two-Ctrl-C/handler restoration, sliced stdout and worker-thread transport are [attributed manual evidence](fix-process-integration/manual-joined.txt), whose complete driver was read. Installer repeated-boundary results are also attributed. No live model/public workflow was repeatedly interrupted by this reviewer. |

## Evidence limits, first harness failures and cleanup

The **907 passed**, repository Ruff, zero-error basedpyright, 458-file format check, installed doctor/59 managed hashes, real native 420-second history/critique-only recovery, and Chrome checks are **reused coordinator/implementation evidence** from [integration](integration.md), [native run](native-run.md), [gallery delivery](gallery-delivery.md) and [critique recovery](critique-resume.md). They predate the final fixes and were not rerun wholesale by this reviewer; the narrow fresh final results below resolve the observed defects. No private session export/system/reasoning content was opened or published by this reviewer; no inference, public artwork change, live profile install, auth/default-config write, commit, push or release occurred.

Initial discovery had an absent `scripts/` path and a nonmatching `*.js` glob; actual helper path and `.mjs` files were then read. These were discovery-command errors, not test failures. Owned harness static checks initially found executable/path/style issues, two implicit concatenations and an unused alarm result; all first diagnostics remain, and only the owned harness was corrected. S101 is excluded for the exact temporary pytest modules to match the repository's existing test policy. [First harness lint](review-code/harness-lint-first.txt), [first types](review-code/harness-types-first.txt), [follow-up first lint](review-code/followup-lint-first.txt), [follow-up first types](review-code/followup-types-first.txt). Final owned-code checks are [lint](review-code/harness-lint.txt), [types](review-code/harness-types.txt), [follow-up lint](review-code/followup-lint.txt), [follow-up types](review-code/followup-types.txt).

Resource registration and incremental observations are preserved in [resource journal](review-code/resource-journal.md). Scratch and runtime resources were removed after both validation phases; the [latest actual cleanup receipt](review-code/cleanup-settled-receipt.json) supersedes the earlier checkpoint. No server, browser, download or package installation was needed by this reviewer. The integration race, installed-source binding and final full suite are verified; no review dependency remains.

## Independent code-ready revalidation

Root authorized the narrow code-ready rerun in `msg_6296f97247e9`, before final installation/static/manual evidence. All four original defects are **resolved for their exact retained reproductions**: [three original boundary regressions](review-code/defensive-fixed-tests.txt) passed in 2.62s and [both installer rollback variants](review-code/install-rollback-fixed-tests.txt) passed in 0.04s. At receipt time the observed descendant/helper PIDs 39488/39547 were already absent, helper ResourceWarnings were zero, the nested foreign marker was refused/preserved, and both I/O/interruption rollback restored the previous install. [Receipt](review-code/defensive-fixed-receipt.json). No expectations changed; original failed artifacts remain.

Read all four final-delta production files (`launcher_process.py`, `helper_process.py`, `installer.py`, new `process_group.py`) and parsed all 51 native files with real Python 3.11.16. [Source pin](review-code/source-fixes-code-ready.sha256), [parser](review-code/source-audit-fixed.json). All 13 protected and 14 public files still matched the original pin at this checkpoint. The context owner's native-run wording correction is acknowledged: old job **fields** match after optional `retry_of:null` default normalization; this is not a claim of raw JSON byte equality across recovery.

### Additional major finding: forced launcher leaves its actual helper's separate group

**The gate was FAIL at this first code-ready checkpoint; the final unchanged reproduction below resolves it.** At that checkpoint `process_group.settle_group` checked only the launcher's PGID, whereas actual `helper_process.run_cli` started its child in a new session/group. If the leader exhausted grace and was forcibly killed, no caller remained to enforce the helper timeout or execute its cleanup. A real `run_cli` inside a bounded leader that deferred SIGINT produced launcher exit -9/timed_out=true while helper PID 42613 remained alive with PPID 1, PGID 42613, state `Ss`. This was a local owned helper surviving a reported settled launcher; remote-provider cancellation is not the assertion.

[Exact first failure](review-code/nested-helper-tests.txt), [source-bound probe](review-code/nested-helper-probe.py.txt), [affected hashes](review-code/nested-source.sha256), [exit receipt](review-code/nested-helper-receipt.txt). The one new test failed in 2.26s under a 20-second parent deadline; both fixture programs self-limit at 10 seconds and the observed helper was explicitly terminated and confirmed absent. This was a narrowly justified follow-up to reading the new utility, not a broad test rerun. The finding was sent to root and the process owner as requested; this reviewer made no production edit. The nested probe's first typecheck import-root diagnostic is retained in [first types](review-code/nested-types-first.txt), and its strict scratch-import configuration yields [zero errors](review-code/nested-types.txt) with [clean Ruff](review-code/nested-lint.txt).

### Routed adjacent findings and current design review

The goal/security reviewers independently exposed a first-rename interruption boundary after the first installer fix. Their exact probes and the installer follow-up's restoration/foreign-occupation cases are **attributed evidence**, not new executions by this reviewer: [follow-up report](fix-installer-boundary.md), [goal original probe GREEN](fix-installer-boundary/goal-green.txt), [security original probe GREEN](fix-installer-boundary/security-green.txt). The follow-up replaces automatic backup deletion with explicit retention when safe restoration cannot complete, preserves the original exception and identifies the retained path. This reviewer read the full new `test_installer_backup_interrupt.py` and changed installer implementation, then verified its final unchanged-source hashes and original rollback tests.

The process owner also found an unbounded `Popen.__exit__` wait after a failed force-settlement deadline; [first owner failure](fix-process/red-force-bound.txt) is attributed evidence. Root's later decision `msg_799670983937` authorizes only numeric PID enumeration of the launcher's new SID, rechecked SID/PGID membership before signaling, and a separate helper PGID in that session. Read the initial and final `process_scope.py`, all three adjoining process modules and the new boundary tests: no Python `preexec_fn`, IPC channel, process-name/argv/environment enumeration, watcher or extra dependency is used. Helper calls outside a launcher retain PGID-only cleanup. The final unchanged assertions are recorded below.

## Final independent revalidation and source integrity

The process owner's `READY_FOR_REVIEW` (`msg_106cf19ad510`) and root's final installer message (`msg_a40cb7613f08`) authorized the final narrow reruns. No original expectation changed. The three-boundary and nested probe files are byte-identical to their first preserved reproductions; installer changes since its first artifact are the previously disclosed harness-only formatting/unused-return/message cleanup.

| Fresh final execution | Exact result |
| --- | --- |
| `PYTEST_ADDOPTS='-o cache_dir=/tmp/logopia-review-code-080/pytest-cache-fixed' uv run --locked python /tmp/logopia-review-code-080/run_assigned.py defensive-final` | **3 passed in 2.65s**, exit 0, 2.818s outer elapsed, no timeout; helper/descendant absent and nested foreign marker preserved. [Output](review-code/defensive-final-tests.txt), [exact argv/receipt](review-code/defensive-final-receipt.json), [runner](review-code/final-test-runner.py.txt). |
| `PYTHONPATH=integrations/hermes PYTEST_ADDOPTS='-o cache_dir=/tmp/logopia-review-code-080/pytest-cache-fixed' uv run --locked python /tmp/logopia-review-code-080/test_install_recovery.py fixed` | **2 passed in 0.03s**, exit 0; I/O and interruption restored the previous install. [Output](review-code/install-rollback-final-tests.txt), [receipt](review-code/install-rollback-final-receipt.json), [probe](review-code/install-final-probe.py.txt). |
| `PYTHONPATH=integrations/hermes PYTEST_ADDOPTS='-o cache_dir=/tmp/logopia-review-code-080/pytest-cache-fixed' uv run --locked python /tmp/logopia-review-code-080/test_nested_helper.py` | **1 passed in 2.33s**, exit 0; forced leader exit -9/timed_out=true, actual helper PID 71528 absent before receipt assertion. [Output](review-code/nested-helper-final-tests.txt), [receipt](review-code/nested-helper-final-receipt.json), [unchanged probe](review-code/nested-helper-final-probe.py.txt). |

Final process/installer hashes matched before and after those executions. [Process pin](review-code/process-final-source.sha256), [process integrity](review-code/process-final-integrity.txt), [installer pin](review-code/installer-final-source.sha256), [installer integrity](review-code/installer-final-integrity.txt). The final 164-file source/test/helper inventory contains exactly three changed original production modules and the authorized CONTRACT clarification, plus [seven announced new source/test files](review-code/source-added-paths.txt). [Final inventory](review-code/source-final.sha256), [original comparison](review-code/source-original-final-check.txt), [final verification](review-code/source-final-integrity.txt). No unexplained source drift was found.

Root explicitly announced the G2 operational clarification, English/Korean shutdown/backup wording and earlier native-run precision correction in `msg_2f4d0c5fda08`. Their [separate doc hashes](review-code/authorized-docs-final.sha256) preserve the distinction from the original pin. The actual native Python 3.11.16 parser accepted all **52** final modules, with no broad-type AST names/helper imports and maximum 229 nonblank/noncomment lines. [Receipt](review-code/source-audit-final.json), [read-only audit script](review-code/native-grammar-audit.py.txt). The final runner's first lint found one literal-argument-count style issue; it is retained in [first lint](review-code/final-runner-lint.txt), with [clean lint](review-code/final-runner-lint-clean.txt) and [zero type errors](review-code/final-runner-types-clean.txt) after exhaustive argument routing. No production formatting/type suppression was performed by this reviewer.

The exact required tmux channel was repeated after the final process changes. Actual success JSON again reported **revision 47 / delivered / e2 / five candidates**, exit 0; malformed `../review-code-inert` returned `invalid_id`, exit 1. [Before](review-code/tmux-final-before.txt), [exact show capture](review-code/tmux-final-show.txt), [malformed capture](review-code/tmux-final-malformed.txt), [joined lines with both exit statuses](review-code/tmux-final-unwrapped.txt). The canonical SHA remained `16eea6dc8c32e979c432fbdffee137d76cb68a230aad9e5b65cc740d59e26e04` [before](review-code/canonical-before-final-tmux.sha256) and [after](review-code/canonical-after-final-tmux.sha256). All 13 protected files and 14 public sample files still matched their original pins at this checkpoint. [Protected check](review-code/protected-final-manual-check.txt), [public check](review-code/public-final-check.txt).

## First completed cleanup checkpoint

The final owned tmux session `logopia-review-code-080` was closed; pane PID **72876** and every recorded original/final fixture PID returned no `ps` rows. `tmux has-session` returned exit 1, “can't find session.” The exact scratch root and its `/private/tmp` alias are absent (both absence checks exit 0), including scripts, synthetic profiles/artwork, basetemps, bytecode and owned pytest cache. Final scripts/config survive only as retained review artifacts. No browser, service, listening port, download or package installation was created, so no port/browser cleanup was applicable. Shared repository/uv caches and other workers' resources were preserved.

The first cleanup attempt, `rm -rf /tmp/logopia-review-code-080`, was **rejected before execution** by the tool's `rm -f` guard. The safer `rm -r /tmp/logopia-review-code-080` succeeded with exit 0; no scope expansion or approval request was used. [Machine-readable actual receipt](review-code/cleanup-receipt.json). This tool rejection was not a test failure and is retained rather than hidden.

## Frozen integration correction: final six-case verification

The additional **major** defect was reproduced by the process owner with a real SIGINT immediately after Popen's waitpid-lock acquisition, on both Python 3.11 and 3.12. This reviewer read the original integration traceback, both stdlib implementations, the complete finite toggle probe, owner RED/GREEN and complete final four-module source/diff. [Independent analysis and attributed receipts](review-code/reap-race-analysis.md). The final guard calls the existing SIGINT handler, retains that handler's original KeyboardInterrupt object and restores the handler before propagation; non-main-thread calls and non-callable policies pass through. Launcher/helper calls retain one absolute deadline with protected waits of at most 0.1s. The existing owned numeric query's poll/kill/reap path is also protected, with its 0.25s bound. No long signal mask, ignored warning, private Popen repair, invented successful exit status, new dependency or widened grace/force budget was introduced.

After owner freeze `msg_ed9dba9542fb` and root acceptance, this reviewer ran exactly:

```sh
uv run --locked python /tmp/logopia-review-code-080/run_settled.py
```

The [fixed runner](review-code/settled-runner.py.txt) invokes the original three boundary tests, the unchanged nested-helper test, and `tests/hermes/test_helper_interrupt.py::test_helper_repeated_caller_interrupts_settle_before_original_propagates` (original False and new True acquisition parameter). **6 passed in 5.45s**, exit 0, no timeout; outer elapsed 5.729s under a 60-second bound. [Exact argv/PID/exit receipt](review-code/settled-receipt.json), [actual observations](review-code/settled-tests.txt). The original repeated case settled in 0.432s; the injected acquisition case was absent/unlocked with its returncode recorded before fixture cleanup. Original exception identity, at least two interrupts and zero warning assertions remained intact. No test was deleted or relaxed.

[Six-file frozen pin](review-code/settled-source.sha256) matched [before](review-code/settled-source-before-check.txt) and [after](review-code/settled-source-after-check.txt). The [complete 164-file final source inventory](review-code/source-settled-all.sha256) also [verified](review-code/source-settled-integrity.txt). Actual native Python 3.11.16 again parsed all 52 files with no broad-type AST names/helper imports, maximum 229 nonblank/noncomment lines. [Parser receipt](review-code/source-audit-settled.json). Owned runner [lint](review-code/settled-runner-lint.txt) and [types](review-code/settled-runner-types.txt) pass; its first implicit-concatenation [lint](review-code/settled-runner-lint-first.txt)/[type](review-code/settled-runner-types-first.txt) diagnostics remain preserved. Only harness selector construction and formatting changed.

The owner's separate actual tmux run verifies two Ctrl-C events with original exception identity and reaping, exact accumulated stdout across multiple communication slices, a real worker-thread helper invocation, handler restoration and zero warnings after garbage collection. [Actual owner channel](fix-process-integration/manual-joined.txt), [driver read by this reviewer](fix-process-integration/manual.py.txt). This is attributed owner evidence, not a second manual claim by this reviewer.

The reviewer's final exact tmux show/malformed replay also passed: **r47/delivered/e2/five candidates, exit 0**, followed by **invalid_id/exit 1**. [Before](review-code/tmux-settled-before.txt), [show](review-code/tmux-settled-show.txt), [malformed](review-code/tmux-settled-malformed.txt), [both exit statuses](review-code/tmux-settled-unwrapped.txt). Canonical SHA is unchanged [before](review-code/canonical-settled-before.sha256)/[after](review-code/canonical-settled-after.sha256). All [13 originally protected files](review-code/protected-settled-check.txt), [14 public files](review-code/public-settled-check.txt) and the [previously verified installer delta](review-code/installer-settled-check.txt) still match.

## Final installed-source binding and cleanup

Root's refreshed private `output/hermes-demo/installed-final-binding.json` records plugin **0.8.0**, revision **47**, **delivered/e2**, **61 managed files**, five originals and three ZIP members. This reviewer independently checked all **61 installed-file hashes** and all **60 corresponding repository payload hashes**; the extra settings file is intentionally installed-local. [Sanitized binding summary](review-code/installed-final-binding-summary.json), [installed checks](review-code/installed-final-files-check.txt), [source checks](review-code/installed-final-source-check.txt), [receipt hashes](review-code/installed-final-receipts.sha256). Root's actual install success, workspace-settings match and unchanged default config are attributed to its verification receipt/pre-post evidence. No private export, credential or raw provider response was copied or published by this reviewer.

After the final phase, `rm -r /tmp/logopia-review-code-080` returned exit 0; both `/tmp` and `/private/tmp` absence checks returned exit 0. The registered tmux session is absent, pane PID **9049** is absent and latest fixture/runner PIDs **6085, 6089, 6251, 6259, 6432, 6442** have no OS rows. [Actual final cleanup receipt](review-code/cleanup-settled-receipt.json). All scripts, fixture profiles/images, basetemps, bytecode and owned test cache under that exact root were removed; retained text evidence is intentional. Earlier PID absence checks remain historical because PIDs can be reused. No owned port, server, browser, download or installed package needed cleanup; shared caches and other workers' resources were preserved. No commit, push or publication was performed.

Root's final `FIXES_READY` (`msg_8f4972033e5f`) closes the integration hold: **957 passed in 295.48s, exit 0**, with all eight final gate exits zero and all **209** pinned production/test/config inputs unchanged. These are coordinator executions, read and attributed by this reviewer, in addition to the fresh independent six-case result: [full tests](final-integration/final-pytest.txt), [Ruff](final-integration/final-ruff.txt), [full types](final-integration/final-types.txt), [native 3.11 types](final-integration/final-native-types.txt), [490-file format check](final-integration/final-format.txt), [lock check](final-integration/final-lock.txt), [saved state](final-integration/final-saved-state.txt), [input/cleanup receipt](final-integration/final-run-cleanup.json). The final [public installed binding](final-integration/installed-binding.json) agrees with this reviewer's independently checked private receipt. The earlier 907-test run is historical evidence, not the final suite count. Publication remains a separate root-owned action and is not an implementation defect.
