# Independent goal and contract review, Logopia 0.8.0

**Final verdict: PASS.** Review-only dispatch `task_570fccd79397` / `ctx_719ab77e5650`; comparison HEAD `f7dd3268313d2422b87efea63eef8257b0e651b9`. Coordinator `FIXES_READY` (`msg_5261224a2f63`, 2026-09-14 18:05:34 UTC) removed the final dependency. The originally observed process/installer blockers and subsequent acquisition-race failure are resolved by source-bound regressions and final integration evidence; their initial FAIL/RED results remain below.

This reviewer independently verified frozen r47/delivered/e2, exact public JSON and ZIP bytes, all required critique/parent bindings, and the final actual installation's **61 managed records: 60 source files plus one settings file**. The coordinator's final **957 passed in 295.48s**, static checks and immutable-input receipt were read and are explicitly attributed, not claimed as this review's execution. [Final own binding](review-goal/final-binding.json), [root test output](final-integration/final-pytest.txt), [root input/cleanup receipt](final-integration/final-run-cleanup.json).

Confidence is **high** for the inspected implementation, bounded failure behavior and exact saved-state/artifact bindings; **moderate** for visual usefulness beyond the one demonstrated brand workflow. Nonblocking limits remain: native app/IP output was not demonstrated, IP prompt framing can be narrower, the working gallery is Korean-only, and duplicate gallery publication adds storage. No new inference, production/source change, profile write or publication was performed by this reviewer. Public release v0.7.0 and the historical failed v0.4.0 draft remain truthful; pending root-owned T6 publication is not an implementation defect.

## Work plan

1. COMPLETED: pinned source, canonical state, protected drafts, and unchanged HEAD at the comparison commit; strict baseline characterization is part of step 3.
2. COMPLETED: read the full implementation and final narrow repairs; trace user requirements, G1–G7, five representative scenarios and all nine adversarial classes.
3. COMPLETED: execute the two authorized test files and exact loopback HTTP checks with bounded owned resources.
4. COMPLETED: final source/actual-install rebind, PASS verdict, evidence limits and actual cleanup recorded; one terminal completion follows this report.

The supplied review plan overrides skill defaults requiring interviews, additional agents, production fixes or other output paths. No nested agents are used. `update_plan` is not exposed in this session; this ledger records the sequential plan.

## Evidence and scope

The review read the plan, full G1–G7 contract, integration/native-run reports, native director and bundled craft/IP guidance, all core/host/helper/launcher/installer/gallery modules and gallery assets, relevant tests, bilingual entry docs, research synthesis and public example. Short native module names below refer to `integrations/hermes/logopia_studio/`; tests refer to `tests/hermes/`. Most of these files are untracked additions; they were read directly instead of treating `git diff` as the feature. Tracked metadata/docs were compared with the specified HEAD. The existing helper remains a separate process boundary; its unchanged source and dedicated compatibility tests were inspected rather than replaced.

Initial execution in this dispatch consists of 17 selected tests, a strict read-only Python verifier, a full allowlisted public projection comparison, the two required real HTTP requests and direct inspection of all five public PNGs. [Verification](review-goal/verification.json), [public binding](review-goal/public-binding.json), [HTTP observables](review-goal/http.json) and [pytest output](review-goal/pytest.txt) are this review's evidence. The verifier reads saved native action metadata only; no private session JSONL export, system message or reasoning content was opened or copied into these artifacts.

The justified installer follow-up adds **one newly executed passing control and one newly executed failing regression**, against the exact accepted installer SHA `768da32cdf90da78fd519a39b9b65bd3bdd4cf81c20dc4e45452ffc3e060708b`. This is separate from the first 17-test pass; [PIN](review-goal/first-rename-pin.txt) and [RED](review-goal/first-rename-red.txt) are preserved. No previously completed HTTP/Chrome scenario or broad suite was repeated.

After owner code-ready, the exact same test bytes were independently re-executed against installer `8f75ba7f123f2550d405f4015de54319453b867044f635e0c7d3ed4b4de02116`: **1 passed in 0.10s**, with the original target and exact previous bytes restored. [Test/source identity](review-goal/first-rename-code-ready.json) and [GREEN receipt](review-goal/first-rename-green-receipt.json) bind that result. The owner's 42-test pass and expanded boundary cases are separately attributed in [installer follow-up](fix-installer-boundary.md).

Coordinator `msg_d65509b2b2de` additionally requested the control and SIGINT cases together on final installer code. That exact unchanged pair freshly **passed 2 tests in 0.07s**, exit 0, PID 67600 reaped; [pair output](review-goal/first-rename-final-pair.txt) and [receipt](review-goal/first-rename-final-pair-receipt.json) preserve it. This explicit follow-up did not widen the test scope.

Final integration supersedes the pre-repair suite: root's [957-test pass](final-integration/final-pytest.txt), [Ruff](final-integration/final-ruff.txt), [490-file format](final-integration/final-format.txt), [full types](final-integration/final-types.txt), [native 3.11 types](final-integration/final-native-types.txt), [lock](final-integration/final-lock.txt) and [saved-state CLI](final-integration/final-saved-state.txt) all exited 0. Root verified all 209 production/test/configuration inputs unchanged across that run. The intermediate [2-failure/954-pass run](final-integration/first-pytest.txt) remains visible and led to a deterministic regression and source repair, not retry-to-green.

The **907-test pass in 270.93 seconds**, repository Ruff/basedpyright/458-file formatting results, real Hermes interpreter import/doctor, actual model runs and Chrome scenarios are **prior execution evidence**, read from [integration](integration.md), [native run](native-run.md), [host](host.md), [critique recovery](critique-resume.md) and [native gallery](native-gallery.md). They were not rerun or inferred from this review's 17 tests. In particular, source/fixture tests alone do not prove native output.

Before probes, [source pins](review-goal/source-before.sha256), [state/protected pins](review-goal/state-before.sha256) and [additional helper/install pins](review-goal/additional-before.sha256) were captured. The initial installed marker contains **59 managed records: 58 plugin source files plus installed-local settings**, all checked against their actual bytes and, for source, this checkout. That is an initial binding, not a claim that subsequent authorized repairs were already installed. Four unrelated `.omo/drafts/logo-land-next-{capability,delivery,metis,tools}.md` files are protected.

Precision note from coordinator `msg_15c696f25231`: recovery preserves the old jobs' values after strict-model default normalization (including optional `retry_of: null`), not necessarily raw JSON serialization bytes. This review compares parsed `Workflow`/`Job` values for that assertion; PNG and final canonical comparisons are byte comparisons. The coordinator additionally authorized a shared `process_group.py` and dedicated regression tests for process settlement, plus a narrow explanatory update to `native-run.md`; those are declared final-delta allowances.

Coordinator `msg_7626c1b6ead0` additionally authorizes final `CONTRACT.md` G2 and bilingual `docs/hermes.md` / `docs/hermes.ko.md` clarification: ten-second graceful settlement plus ten-second forced verification and bounded PID probes; owned session/helper-group scope; and retained installer recovery path `plugins/.logopia-install-*/previous`. The docs do not claim that CLI JSON exposes exception notes. These announced changes must be rebound separately from initial pins; no model/tool/schema/API changes are declared.

## User requirement assessment

ACHIEVED means the implemented behavior has the stated evidence. PARTIAL identifies a bounded limitation or a narrower demonstration, not an unreported failed native call. MISSED would mark a currently blocking requirement; none remain on the final bound source. Initial missed guarantees are preserved as resolved findings instead of overwriting their failure evidence.

| Requirement | Status | Specific evidence and practical limit |
| --- | --- | --- |
| Research Hermes, logo craft, design and UX before implementation | ACHIEVED | [Research synthesis](../../research/hermes-design-workflow.md), plan T1, director craft/IP resources and native API adapter. Research informs prompts and workflow; generated strategy is identified as a proposal, not measured market research. External links were inspected as recorded research evidence, not independently re-researched during this frozen review. |
| Real Hermes-directed workflow beyond a Codex skill | ACHIEVED | Native `plugin.yaml`, `__init__.py`, `registration.register`, `HermesHost.plan/generate/critique`, and the private action bindings checked by the verifier. The actual saved run has one planning job, five image jobs and independent critiques through `openai-codex`; no second provider client or raw provider HTTP appears in the adapter. |
| Concise brief and saved strategy | ACHIEVED | `models_brief.StudioBrief/Strategy`, `host.plan` and `engine_pipeline.produce`; exact product/audience/personality/use case and lettering are preserved. Defaults and unsupported inputs passed selected model tests. Strategy assumptions are explicit. |
| Meaningfully distinct directions | ACHIEVED | Three actual originals: c1 diagonal offcut, c2 upright repair joint, c3 staggered modular pieces. `Direction` stores construction, rationale, risk and preserve intent; `host.plan` checks agreed count and distinct identifiers/constructions. Visual distinctness was inspected, not converted into a quality score. |
| Native original PNGs and provenance | ACHIEVED | `host.generate`, `host_images`, `helper.import_job`; all five decoded originals, receipt attribution, dimensions and public byte equality checked. Initial jobs have zero parent inputs, edits have one. Requested square size does not override actual 1774×887 or 1254×1254 return sizes. |
| Concrete pixel critique at use size | ACHIEVED | `host.critique`, `checks.verify_critiques`; two separate roles see actual original and 192 px raster view. All ten saved critiques match image/parent/view bindings and use unique calls. c1 spacing and e1 protected-gap failures remain visible. |
| Explicit user choice distinct from model pass | ACHIEVED | `engine_actions.choose/deliver`, `checked_feedback` and native gallery draft behavior. c1 was selected with unmet lettering criteria; selection did not authorize delivery. The demo identifies both selections as the coordinator's sample choices, not an actual user's approval. |
| Exact-parent revision with retained keep/change | ACHIEVED | `engine_actions.revise`, `edit_prompt`, `host.generate` and immutable jobs. Actual c1→e1→e2 hashes and one-parent native receipts match; two fresh e2 critics replace no e1 history. Exact parent transport is proven; pixel-identical preservation of every glyph is not promised. |
| Three brand/app defaults, six IP defaults; explicit 1–6 | ACHIEVED | `StudioBrief.effective_count`; new verifier confirms 3/3/6 and endpoints 1/6. Selected tests reject 0, 7, boolean, string and float counts, without clamping. |
| At most two edit calls, including failures/unknowns | ACHIEVED | `engine_actions.revise`, persisted jobs, actual two-edit chain; existing `test_core_adversarial.py` checks two failed edits and unknown second edit across restart. Those broader tests are inspected/prior evidence, not newly executed here. |
| Supported brand/app/IP scope | PARTIAL | Native brand, centered pictogram app and lower-corner IP routes exist; app/IP require no exact text and a filled square. The real demo proves brand behavior only. Other existing monogram/soft3D/pixel presets remain on the broader skill path. IP final prompt assembly retains use-case data despite the bundled recipe's narrower framing (F5). |
| Bounded recovery without replacement images | ACHIEVED | `can_resume_critique`, launcher preflight and repeated-start guard. The retained native r23 timeout state progresses through one explicit critique retry to r27 with zero new images/planning and unchanged first seven jobs. Unknown image/plan/edit/delivery are not silently retried. |
| Settled cancellation, finite commands, isolated install | ACHIEVED | Final `launcher_process`, `helper_process`, `process_group` and `process_scope` distinguish leader reap from owned-group absence, preserve invocation/cleanup deadlines and defer SIGINT over bounded Popen operations. Installer preserves prior bytes or a recovery backup across both moves. Own unchanged first-rename pair passed; [independent six-case process check](review-code/settled-tests.txt), [owner 39-case check](fix-process-integration/green-scoped.txt), final 957-test integration and [actual install binding](review-goal/final-binding.json) resolve F1–F4/F9–F12. |
| Intuitive comparison, inline target/parent details, feedback | ACHIEVED | `gallery*`, `assets/gallery*`: next action, strategy, white original cards, separate reviews, parent links, immutable workflow/revision/hash draft, direct PNG links. Prior native Chrome evidence covers 390 px, keyboard, clipboard denial, reload and no external requests. Fresh review used literal HTTP and direct PNG inspection, not a new browser run. |
| English-default concise docs, Korean counterpart | ACHIEVED | Root/docs READMEs, [Hermes guide](../../hermes.md), [Korean guide](../../hermes.ko.md), English public sample and Korean sample README. Detailed technical material sits behind short entry points. The generated working gallery itself remains Korean-only (F6). |
| Immediately visible white samples and actual delivery | ACHIEVED | README previews and public page directly expose actual originals; all five were inspected without modifying pixels. HTTP JSON/ZIP and required-review lineage match frozen r47/e2. ZIP includes exact PNG, manifest and guide, not placeholder text or a renamed archive. |
| IP reference credit | ACHIEVED | Director `references/ip.md` cites pinned upstream `acb834c717bcd0a487c49732d08397ba280d690b`; bundled MIT license retains copyright and permission notice. No upstream showcase image or executable is copied. |
| Existing skill/helper compatibility and truthful limits | ACHIEVED | Independent workflow schema 1, unchanged helper session schema 2, locked CLI bridge; tracked pins and existing-helper test/source review support additive scope. Advisory palette only; strict palette uses existing skill/helper. No vector/font files, trademark clearance, human expert certification or measured aesthetic superiority is claimed. |
| Preserve defaults, credentials and unrelated work | ACHIEVED | Explicit named-profile CLI only; final 61-record actual-install/source check and settings equality passed. This reviewer directly rehashed the default config against the retained pre-install digest. Canonical, helper session, four drafts, 14 public files and all 1,862 initially pinned tracked files are unchanged. [Final binding](review-goal/final-binding.json); no config/auth/profile/source writes by this reviewer. |
| Publish 0.8.0 after reviews | PARTIAL | Root-owned T6 is pending by design. The truthful release history is retained; no release action belongs to this dispatch. This does not block implementation acceptance after actual defects settle. |

## Contract G1–G7

| Contract | Status | Trace and evidence |
| --- | --- | --- |
| G1: Python/dependencies/helper boundary | ACHIEVED | `plugin.yaml`, `installer_models`, `launcher_install`, `helper.invoke` use required Pydantic/Pillow ranges and exact `uv run --locked --project … python …logo_project.py --workspace …` argv. No Python 3.12 helper import in the core. Prior native Python 3.11 import/doctor/typecheck is recorded in integration; this review's strict 3.11-targeted temporary verifier typecheck has zero errors. Missing dependencies are reported, not implicitly installed. |
| G2: reservations, bounded calls, interrupt/reconcile and settlement | ACHIEVED | Persisted pre-call identity, 120 s roles and 6000/2400 token bounds, serial image calls and native recovery/counts remain. Final group/session teardown and acquisition-race fix pass unchanged original and targeted real-SIGINT regressions plus actual tmux. Launcher cleanup is at most configured grace + 10 s force plus bounded numeric-PID probe cleanup; exhausted/unverifiable settlement fails explicitly. Repeated interrupts do not reset deadlines. Provider retry count/cancellation and arbitrary software escaping the owned SID are not guaranteed. [Process repair](fix-process.md), [race repair](fix-process-integration.md). |
| G3: exact helper commit recovery/export preservation | ACHIEVED | `helper.import_job`, `helper_delivery.export_job/verified_delivery`, `engine_recovery.reconcile` bind deterministic IDs, parent/prompt/intent, original/hash and export destination. Existing helper recovery tests and prior core evidence cover committed-import/export interruptions; the real delivery and exact ZIP were newly verified. These are recoverable two-store operations, not atomic cross-file transactions. Final installer restoration/backup retention also passes F3/F4/F11. |
| G4: actual-pixel bindings and two-role required checks | ACHIEVED | `host.critique` computes call/image/parent/view/parent-view hashes; model only returns strict criteria/summary. `checks.verify_critiques/unmet_criteria` rejects missing, duplicate, mismatched, not-observed and invalid N/A. New canonical verification confirms all bindings, e1 preservation failure and e2 all-required pass; helper manifest review/source matches e2. |
| G5: advisory palette and fixed revision intent | ACHIEVED | `models_brief.color_policy`, director instructions, `prompts`, import `--background`; selected tests reject strict color policy and forbidden app/IP intent. Exact text/colors/background stay on the saved brief. Natural-language semantic conflict detection, exact font identity and hex equality are explicitly outside the guarantee. |
| G6: strict feedback/revision/hash and safe offline UX | ACHIEVED | `models_review.FeedbackEnvelope`, `host_requests`, `launcher_requests`, `registration.checked_feedback`, store CAS and gallery JS. New probes reject duplicate keys, 32 KiB overflow, boolean revision and stale r46; instruction-like content remains literal data. Prior browser evidence confirms copy fallback/no-JS/direct originals; gallery local selection does not mutate canonical state. |
| G7: observed 420 s failure and bounded explicit critique-only continue | ACHIEVED | First failure remains in native-run; `launcher_install` configures only named-profile sequential/concurrent limits to 1800 via public CLI. `launcher_runner._settled_state` distinguishes an unfinished exit from an actual deadline. Original j7 remains unknown, j8 is its explicit critique retry, images and plan unchanged. New selected resume guard tests reject implicit start retry in failed/unknown/cancelled states. Late/superseded result checks are inspected in `engine_production` and dedicated prior tests. The separately required G2 process repairs are resolved on the final bound source. |

## Representative scenario traces

1. **Brand brief → three directions → actual originals.** Follow `registration.logopia_start` → strict saved brief → `Studio.produce` → strategist/art-director calls → three reserved image jobs/imports → two-role review per candidate. Saved jobs j2/j3/j4 are the only initial calls. I inspected all five PNGs and verified these three bytes against canonical originals and public copies. c1/c2/c3 differ in construction, not just captions or color. This is a real stored native run, revalidated without recreating it.
2. **Failed critique → explicit bounded recovery.** Read strict `before-recovery-workflow.json` r23, verify `can_resume_critique`, compare final first seven jobs unchanged, then inspect j8 `retry_of=j7` and unchanged candidate image/prompt/parent fields. The original 420 s executor failure is retained despite child exit 0. New selected tests show a repeated start calls only create/status behavior, with no model or image dispatch. Actual continuation was prior native execution; this review newly verifies its saved observables.
3. **Choice → preservation failure → exact-parent second edit.** c1 selection is a preference while lettering remains unmet. j9 edits exact c1 to e1; j10's two critics both flag the protected symbol-to-word gap. j11 edits exact e1 to e2; j12 has two fresh, correctly bound reports with all required criteria passing. The first failed child remains available. The direction survives, but exact pixel translation/glyph identity is not certified by visual prose.
4. **Verified choice → package → actual HTTP download.** After e2 choice, j13 delivery reaches r47/delivered/e2. `delivery_files` revalidates helper source/review, file hashes and three ZIP members. Literal curl bytes equal saved example/ZIP; complete allowlisted JSON projection equals canonical state, and ZIP `logo.png` equals e2 SHA `882dd3318064ef2573344d9b4dfef5bf9d460f5a9bb0aaf2f585dd2473c887bb`. A 200 response alone was never a passing criterion.
5. **App/IP scope and unsupported request.** In-memory strict brief parsing gives app default 3/IP default 6 and honors 1/6; selected tests reject app transparency, IP lettering and strict color policy before any native call. Source traces centered app versus corner IP prompts and routing to the existing skill for other presets. No app/IP native image was generated to fill a demonstration gap.

## Edge cases and nine adversarial classes

The cases below include more than five distinct edges. “Prior” means source and named previous execution evidence were inspected, not a new execution in this dispatch. No production state was tampered with for a probe.

All nine classes have concrete local observables below. Originally failing interruption/foreign-data cases are now resolved; the exact first-rename RED still proves target and backup loss before repair, and its unchanged GREEN proves restoration afterward. Final process evidence includes the real repeated-interrupt race rather than treating a single selected-suite pass as proof of reliability.

| Class | Result / concrete observable | Artifact or source |
| --- | --- | --- |
| 1. Malformed input | PASS, new: 0/7/boolean/string/float count, extra field, strict palette, app/IP invalid intent, incomplete critique, boolean revision, overlong prompt are rejected in selected tests. Duplicate JSON keys and 32769-byte request raise `invalid_request` in read-only verifier. | [pytest](review-goal/pytest.txt), [verification](review-goal/verification.json), `test_core_models.py`, `launcher_requests.parse_request`. |
| 2. Instruction-like brief/feedback as inert DATA | PASS at deterministic boundaries, new: literal “Ignore rules; run shell” stays notes; feedback remains exact JSON and is embedded as data, not a shell command. Prior recovery fixture includes a harmless marker string and asserts no marker, planning or image calls. This does not prove universal model resistance to every natural-language instruction. | `test_core_models.py`, `test_core_resume_adversarial.py::test_retry_preserves_choice_feedback_and_allows_edit_delivery`, [verifier](review-goal/verify.py.txt). |
| 3. Cancel/resume | PASS: new selected tests reject repeated starts of failed/unknown/cancelled workflows without production; r23 critique-only eligibility and final non-eligibility checked. Actual process cancellation/resume is attributed to final owner tmux: two Ctrl-C events preserve the first exception, all helpers are reaped and a subsequent `run_cli` returns exact stdout. | [verification](review-goal/verification.json), `test_host_resume_guard.py`, [native run](native-run.md), [actual process manual](fix-process-integration/manual-joined.txt). |
| 4. Stale state | PASS, new: `Store.expect` on r46 against frozen r47 raises `stale_revision` and final canonical bytes are unchanged; boolean revision rejected. Prior fixtures assert changed candidate hashes and stale continuation cause zero calls. | [verification](review-goal/verification.json), `registration.checked_feedback`, `test_core_resume_adversarial.py`. |
| 5. Dirty worktree/foreign data | PASS final: own pins prove unchanged HEAD/tracked files, four unrelated drafts and canonical/native/public data. Own first-rename SIGINT probe initially failed, then exact unchanged control+SIGINT pair passed. Attributed final nested-marker probe returns `modified_install` and preserves the foreign bytes; owner tests retain previous backups when restoration is unsafe. | [final binding](review-goal/final-binding.json), [own pair](review-goal/first-rename-final-pair.txt), [independent marker probe](review-code/settled-tests.txt), [installer boundaries](fix-installer-boundary.md). |
| 6. Hung/long commands | PASS final: original leader/helper/nested/forced-wait failures and later lock race were preserved and repaired. Independent final probes observe no owned descendant/helper before receipt; denied signalling returns bounded failure, not success. All this review's finite commands settled; the historical actual 420 s failure is retained. | [independent final six cases](review-code/settled-tests.txt), [owner denied-signal bounds](fix-process/green-nested-boundaries.txt), [race manual](fix-process-integration/manual-joined.txt), [own receipts](review-goal/pytest-receipt.json), [cleanup](review-goal/cleanup-rebind.json). |
| 7. Flaky tests/first-failure handling | PASS for handling an actual intermittent failure: root's 2-failure/954-pass run exposed a real lock/interrupt race, reproduced deterministically on 3.11 and 3.12 with real SIGINT and a passing control. A source fix, unchanged regressions and final 957-pass run followed. Own selected 17 passed first/only run. N/A narrowly: these runs do not prove absence of every nondeterministic flake or repeatability of stochastic aesthetics. First harness failures remain, including Ruby 2.6 compatibility. | [first integrated failure](final-integration/first-pytest.txt), [race evidence](fix-process-integration.md), [final full run](final-integration/final-pytest.txt), [first Ruby failure](review-goal/final-binding-first-error.txt), resource ledger. |
| 8. Misleading success | PASS, new: empty saved draft with a start request raises `postcondition_failed`; exact HTTP JSON/ZIP body and required lineage checks pass beyond 200/exit 0. Both e1 preservation failures fail `unmet_criteria`; e2 passes matching current checks. Prior native exit 0 with unfinished j7 was correctly retained as unknown. | [verification](review-goal/verification.json), [public binding](review-goal/public-binding.json), [HTTP](review-goal/http.json), `launcher_state.verify_result`. |
| 9. Repeated interruptions | PASS: persisted two-edit budget and late-return/cancel guards remain supported by inspected prior tests; new selected start/resume guards passed. Final attributed real-SIGINT original/acquisition cases preserve first exception identity, released poll lock, PID/PGID absence and zero warnings. Own installer real-SIGINT original pair passes; owner expanded tests preserve prior bytes through failed restoration/foreign occupation. | `test_core_adversarial.py`, `test_core_critique_late.py`, [independent process cases](review-code/settled-tests.txt), [actual two Ctrl-C manual](fix-process-integration/manual-joined.txt), [own installer pair](review-goal/first-rename-final-pair.txt). |

## Findings and actionable constraints

| ID | Severity / status | Finding and next action |
| --- | --- | --- |
| F1 | Blocking initially / RESOLVED | Original launcher returned after leader exit with a live same-group descendant. Final settlement requires both leader reap and group absence; independent final descendant PID 6089 is absent before the receipt. [Original module](review-goal/launcher_process.before.py.txt), [final unchanged regression](review-code/settled-tests.txt). |
| F2 | Blocking initially / RESOLVED | Original helper survived caller `KeyboardInterrupt`. Final `finally` settlement and bounded interrupt guard preserve the first exception after cleanup; independent helper PID 6251 is absent with ResourceWarnings 0. [Original module](review-goal/helper_process.before.py.txt), [final unchanged regression](review-code/settled-tests.txt). |
| F3 | Blocking initially / RESOLVED | Original installer exempted any nested `.logopia-install.json` by basename and could delete foreign bytes. Final exemption is exact-root only; final independent probe returns `modified_install` and preserves the nested sentinel. [Original module](review-goal/installer.before.py.txt), [final marker observation](review-code/settled-tests.txt). |
| F4 | Blocking initially / RESOLVED | Interrupted new-target publication bypassed ordinary-error rollback and deleted the previous install. Both original I/O and interruption probes now restore previous bytes; final installer additionally retains unresolved backup data. [Initial rollback failure](review-code/install-rollback-tests.txt), [original probe](review-code/install-rollback-probe.py.txt), [final two-case result](review-code/install-rollback-final-tests.txt). |
| F5 | Low / nonblocking | `prompts.image_prompt` excludes background/logo_type but retains full project/product/use_case JSON for IP. Bundled `references/ip.md` says the final image prompt should contain only visual character ingredients and avoid use-case framing. A no-inference probe confirms the mismatch. Future work can render IP visual direction fields separately while keeping product context in planning. No native IP output failure is established. |
| F6 | Low / nonblocking | `assets/gallery.html` hardcodes Korean, with Korean labels in gallery modules. English docs and public example are present, but an English-first user receives a Korean working gallery. A future locale/English default would align the entire interaction; bilingual-doc acceptance is already satisfied. |
| F7 | Low / nonblocking | Native registration publishes a complete immutable gallery, then launcher `_finish` publishes another fresh gallery for the same result. Full original copies and inline download payloads make this redundant storage visible. Reuse one verified snapshot reference where possible; retain failed-run gallery fallback and explicit offline downloads. Maximum candidate/edit budgets bound each snapshot, but repeated actions accumulate snapshots. |
| F8 | Informational | Model critiques cannot certify pixel-exact lettering, a measured 10–12 px translation, exact hex palette, legal originality, font licensing or general professional superiority. The public sample labels recorded model observations and excludes vector/font/stamp assets. Keep these limits visible; do not turn e2's model pass into a benchmark result. |
| F9 | Blocking initially / RESOLVED | Actual nested helper formerly escaped forced launcher cleanup in a new session. Final helper owns a separate PGID within the launcher's fresh SID; numeric SID/PGID checks precede signalling. Independent nested helper PID 6259 is absent after launcher -9/timed-out receipt. [Initial failure](review-code/nested-helper-tests.txt), [unchanged final probe](review-code/settled-tests.txt). |
| F10 | Blocking initially / RESOLVED | `Popen.__exit__` could wait past the forced deadline when group signals were rejected. Final code owns streams directly/through `ExitStack` without an implicit Popen wait. Finite denied-delivery fixtures return explicit failures at 1.208 s launcher / 2.304 s helper, then fixtures clean their exact PIDs. [Initial failure](fix-process/red-force-bound.txt), [bounded final observations](fix-process/green-nested-boundaries.txt); existing cases also pass the final 39-case/full runs. |
| F11 | Blocking initially / RESOLVED | This reviewer proved first old-target→backup rename followed by actual SIGINT lost both prior target and backup. Final installer wraps both moves and retains unrecoverable backup bytes. Own byte-identical original control+SIGINT pair now passes 2/2 on installer `8f75ba7f…`, present in the final actual installation. [Initial source](review-goal/installer.first-rename.before.py.txt), [probe](review-goal/first-rename-probe.py.txt), [RED](review-goal/first-rename-red.txt), [final pair](review-goal/first-rename-final-pair.txt), [binding](review-goal/final-binding.json). |
| F12 | Blocking initially / RESOLVED | Root's 2-failure/954-pass integrated run exposed cleanup timeout and delayed Popen warning for PID 78509. Real SIGINT at lock acquisition deterministically reproduced unreaped/locked state on 3.11 and 3.12; final bounded SIGINT deferral protects Popen operations without widening deadlines or modifying private state. Attributed original/targeted six-case, 39-case, real tmux and final 957-case results pass. The old full trace alone does not identify its exact interrupted instruction. [Initial full failure](final-integration/first-pytest.txt), [confirmed boundary/repair](fix-process-integration.md), [independent final six](review-code/settled-tests.txt), [full final](final-integration/final-pytest.txt). |

F1–F4 and F9/F10 were communicated by the coordinator, with independently observed failures owned by the code/process reviewers. F5–F7 are this review's source/UX observations; F11 is this review's newly observed runtime failure. F12's full-run failure and subsequent runtime reproductions are attributed to the root/process/code owners; this reviewer read their exact artifacts and the full final source. The workflow's separate state/helper commit recovery and strict bindings add complexity for a concrete interruption problem; they are justified. Duplicate gallery publication is avoidable overhead. A new generalized agent framework, palette engine or aesthetic scoring loop would exceed the intended bounded workflow.

## Exact executed checks and observables

All commands ran from the shared checkout. Test environment additions were `PYTHONDONTWRITEBYTECODE=1` and `PYTEST_ADDOPTS=-o cache_dir=/tmp/logopia-review-goal-080/pytest-cache`; selected test files and assertions were unchanged.

```sh
uv run --locked pytest -q tests/hermes/test_core_models.py tests/hermes/test_host_resume_guard.py --basetemp /tmp/logopia-review-goal-080/pytest
curl -i --connect-timeout 2 --max-time 15 http://127.0.0.1:8813/docs/hermes-demo/example.json
curl -i --connect-timeout 2 --max-time 15 http://127.0.0.1:8813/docs/hermes-demo/delivery/logo-package.zip
PYTHONPATH=integrations/hermes uv run --locked python /tmp/logopia-review-goal-080/verify.py
ruby /tmp/logopia-review-goal-080/public_binding.rb
uv run --locked pytest -q -s -c pyproject.toml /tmp/logopia-review-goal-080/test_first_rename.py --basetemp /tmp/logopia-review-goal-080/final-pair
ruby /tmp/logopia-review-goal-080/final_binding.rb
```

The server was `.venv/bin/python -m http.server 8813 --bind 127.0.0.1 --directory /tmp/logopia-review-goal-080/http-root`, exposing only a symlink to public `docs/hermes-demo`, never the private project root. Exact curl response bodies were saved in scratch for byte comparison. HTTP JSON was 38,785 bytes, SHA `415184581278b1317132c6c380b5255081bb9146dd02db9a1e227f67b4c624fe`; ZIP was 711,451 bytes, SHA `0d1a4c2266ad6f04b3d1b78ab32c41b89ed5bd6c3b0c0d514fd580a9513d7c04`. Both returned 200 and exact saved bytes. Strict canonical SHA is `16eea6dc8c32e979c432fbdffee137d76cb68a230aad9e5b65cc740d59e26e04` at r47/delivered/e2.

Pytest exited 0 with **17 passed in 0.10 s**. Final authored-harness Ruff and 3.11-targeted basedpyright passed with zero errors; see [lint](review-goal/verify-lint.txt), [types](review-goal/verify-types.txt), [verifier source](review-goal/verify.py.txt), [state verifier](review-goal/verify_state.py.txt), [projection source](review-goal/public_binding.rb.txt) and [strict config](review-goal/pyrightconfig.json.txt). The original failed lint run and import-path correction remain separate evidence; no failed test was deleted, assertion weakened, artwork retried or source formatted.

## Final repair review, binding and cleanup

Named `FIXES_READY` was received after the final whole-suite/installed binding passed. I read all four final process modules, final installer and the full five added regression files, then compared the original full-file pins. No unexplained implementation drift remains: three original production modules plus the announced G2 contract changed; two process modules and five dedicated tests were added. The bilingual guide operational clarifications are separately pinned. Native workflow/model/tool APIs, core Python 3.11 and locked helper 3.12, image/review budgets, exact-parent transport and provider calls are unchanged.

The final process design addresses observed failures: separate leader/group settlement, numeric membership checks within one owned SID, bounded cleanup failure, and at most 0.1 s interrupt-deferred wait/communicate slices under the original absolute deadline. It uses no IPC broker, watcher, process-name scan, raw provider client, private Popen mutation or warning suppression. Installer preserves the original target when restoration succeeds and exact `previous` bytes under `plugins/.logopia-install-*/previous` when restoration is unsafe. These narrower repairs are justified; a general process supervisor or aesthetic scoring framework would exceed this scope.

[Own final rebind](review-goal/final-binding.json) newly verifies every installed digest, every source copy and installed-local settings. [Installed manifest hashes](review-goal/installed-final.sha256), [final native/test inventory](review-goal/native-final.sha256), [seven additions](review-goal/source-added.json), [source comparison](review-goal/source-after.sha256), [protected/state comparison](review-goal/state-after.sha256) and [public hashes](review-goal/public-final.sha256) preserve the evidence. The default config directly matches retained pre-install SHA `2bd35fc1d58229630b5a625d7cc7c48ca50bfbd9cfd6fc23e5af3a29798635bd`. All 1,862 initially pinned tracked files, four unrelated drafts, helper session, canonical and all 14 public files are unchanged. The public JSON/ZIP and native hashes therefore carry the original strict HTTP/critique-lineage proof forward without a new browser, HTTP or model run.

Final actual cleanup **PASS**: [latest receipt](review-goal/cleanup-rebind.json) and [exact removed-file receipt](review-goal/cleanup-rebind-files.json) show the sole scratch root absent, all **ten** recorded child PIDs absent (`ps` exit 1, empty output), and port 8813 free (`lsof` exit 1, empty output). The HTTP child was stopped/reaped; every bounded test/verifier completed and was reaped. Scratch scripts/config, all basetemps/caches, public response copies and the HTTP link/root were removed. No browser profile, tmux session, package installation or virtual environment was created by this reviewer; user Chrome/default profile/credentials and other workers were untouched. All retained scripts are text evidence copies.

Historical cleanup checkpoints remain [initial](review-goal/cleanup.json), [installer GREEN](review-goal/cleanup-final.json) and [final installer pair](review-goal/cleanup-final-pair.json); each later resource recreation was registered before it happened. The [first cleanup guard failure](review-goal/cleanup-first.txt) refused before deletion because macOS resolves `/tmp` to `/private/tmp`; the corrected exact-parent guard completed safely. The last rebind's [first Ruby 2.6 failure](review-goal/final-binding-first-error.txt) and [original source](review-goal/final_binding.first.rb.txt) remain; the equivalent `map.compact` correction changes no assertion. Final [reader source](review-goal/final_binding.rb.txt) passed in 1.118 s, exit 0, under its 60 s bound.

## Resource ledger

- TODO, before creation: `docs/qa/hermes-workflow/review-goal/` will hold sanitized review evidence only and will remain as the requested artifact directory.
- TODO, before creation: `/tmp/logopia-review-goal-080` is the sole owned temporary root; remove its exact contents and root after preserving sanitized results. It will contain baseline file lists, bounded-command receipts, pytest basetemp and local HTTP response bodies. No model call, installer, credential access, artwork change, browser, profile or tmux resource is authorized or planned.
- CREATED: both registered directories; baseline manifests captured before probes.
- TODO, before start: one owned Ruby deadline wrapper and `uv run --locked pytest -q tests/hermes/test_core_models.py tests/hermes/test_host_resume_guard.py --basetemp /tmp/logopia-review-goal-080/pytest`, with a 180-second process-group deadline and 5-second termination grace. Record PID/exit; remove its basetemp and cache after settlement. `PYTHONDONTWRITEBYTECODE=1` and `PYTEST_ADDOPTS=-o cache_dir=/tmp/logopia-review-goal-080/pytest-cache` isolate generated caches; no test selection changes.
- SETTLED: pytest PID 10271 exited 0, 17 passed in 0.10 seconds; no deadline hit.
- TODO, before creation/start: `/tmp/logopia-review-goal-080/http-root/docs/` with one owned `hermes-demo` symlink to public samples only; a loopback-only HTTP server on `127.0.0.1:8813`, owned child PID recorded before requests, 90-second absolute deadline with 5-second termination grace. Two literal curl requests use 2-second connect/15-second total deadlines; response bodies stay in scratch and sanitized headers/hashes remain in review evidence. Stop and reap the exact server immediately after capture, then remove the link/root and response copies during final cleanup.
- SETTLED: HTTP child PID 14382 was command-verified, stopped by SIGTERM and reaped; both curl processes exited 0. Exact response-body equality passed; lineage verification follows.
- TODO, immediately before creation: `/tmp/logopia-review-goal-080/verify.py`, a read-only typed Python verifier for strict canonical/snapshot parsing, original and review bindings, saved native receipt metadata, installed payload hashes, public-vs-native package equality, and bounded no-inference edge probes. Apply `omo:programming` Python guidance; execute with the existing locked environment and a 120-second deadline, check only this authored temporary script, preserve a sanitized source copy as `review-goal/verify.py.txt`, and delete the executable scratch copy at cleanup.
- First verifier execution passed with no canonical change or inference. Initial authored-script lint failed (formatting, main complexity, executable flag, fixed scratch path, numeric labels, stdout style); the full first source and log are retained. These are review harness defects, not product failures. One shell inventory command also failed because a `docs/hermes-demo/*.mjs` glob matched no files; use an explicit file inventory instead.
- TODO, immediately before creation: `/tmp/logopia-review-goal-080/verify_state.py`, an extracted read-only state/recovery check module to keep authored temporary modules under the programming skill's 250-line bound. Preserve `review-goal/verify_state.py.txt`, run scoped lint/types, and remove the scratch module at cleanup.
- TODO, immediately before creation: scratch `__init__.py` and `pyrightconfig.json` for standalone-module discovery under the exact temporary root; remove at cleanup. The first extraction exposed a missing standalone import search path and type-only import style; its logs are retained separately, with no assertion removed or softened.
- TODO, immediately before creation: scratch `public_binding.rb` compares every allowlisted public candidate/review/direction field with canonical state and HTTP JSON, without reading session exports; retain its text and receipt only. Execute under a 30-second deadline, then delete the scratch copy.
- SETTLED AND REMOVED: both final Python checks and public projection passed; exact source copies, type/lint logs and receipts remain in the owned evidence directory. The whole registered scratch root was removed only after each retained copy matched. The [inventory first failure](review-goal/inventory-first.txt) and earlier harness failures remain visible. See [actual cleanup receipt](review-goal/cleanup.json).
- CLEANUP HARNESS CORRECTION: the first realpath guard refused before deletion on macOS; the corrected exact-parent check passed. The initial refusal and final actual cleanup are both retained, without changing production source or tests.
- FOLLOW-UP RESOURCE REGISTRATION: [installer boundary journal](review-goal/installer-boundary.md) registered the same scratch root, one new pytest probe and bounded PIN/RED children before creation. This intentionally follows the already completed initial cleanup receipt. Only synthetic profiles were written; the accepted production installer remained unchanged.
- FOLLOW-UP SETTLED AND REMOVED: original probe passed unchanged on frozen installer, then all PIN/RED/GREEN basetemps/cache and script were removed with the exact owned root. Eight recorded child PIDs are absent and port 8813 is free; [final cleanup receipt](review-goal/cleanup-final.json) is authoritative for that checkpoint.
- FINAL REBIND TODO, before creation: recreate only `/tmp/logopia-review-goal-080` and `final_binding.rb` for a finite, read-only file-hash verifier (60-second absolute deadline, no child processes, inference or profile writes). Read the final actual managed marker, compare all 61 file hashes and 60 source copies, validate installed-local paths, compare original source/tracked/state pins and unchanged public artwork, and save sanitized metadata plus a `.rb.txt` evidence copy under this report's owned directory. Remove the exact scratch script/root after completion and record a new cleanup receipt; existing historical receipts remain.
- READ HARNESS NOTE: a final `wc -l` inventory named nonexistent `fix-process-settlement.md` and exited 1; the actual artifact is `fix-process.md`, discovered with `rg --files`. No check or test was deleted, and the first inventory output is retained in `review-goal/final-inventory-first.txt`.
- FINAL REBIND SETTLED: read-only Ruby verification passed in 1.118s, PID 17885 reaped, with 61 actual installed hashes / 60 exact source matches and no default-config, canonical, helper-session, protected-draft or public-sample drift. The initial Ruby 2.6 `filter_map` compatibility failure is retained in `final-binding-first-error.txt`; the equivalent `map.compact` correction changed no assertion. The full tracked 1,862-file baseline is unchanged. Final source additions are exactly two process modules plus five dedicated tests; original changes are the announced three production modules and G2 docs. Both bilingual guide changes are separately bound.
- FINAL REBIND CLEANUP: the exact script/root were removed only after their text matched the retained evidence. `cleanup-rebind.json` records all ten known child PIDs absent and port 8813 free; no browser, tmux or package installation was created. This is the latest cleanup receipt and supersedes the earlier nine-PID checkpoint.
