diff --git a/docs/qa/hermes-workflow/fix-installer-boundary/interruption.before.py.txt b/tests/hermes/test_installer_interruption.py
index f222b4d..f8356e1 100644
--- a/docs/qa/hermes-workflow/fix-installer-boundary/interruption.before.py.txt
+++ b/tests/hermes/test_installer_interruption.py
@@ -108,6 +108,7 @@ def test_interruption_does_not_overwrite_foreign_replacement(
     monkeypatch: pytest.MonkeyPatch,
 ) -> None:
     # Given an unrelated fixture writer occupying the target after the old directory moved.
+    before = _snapshot(installed.target)
     original_rename = Path.rename
 
     def replace_then_interrupt(path: Path, destination: str | Path) -> Path:
@@ -123,4 +124,8 @@ def test_interruption_does_not_overwrite_foreign_replacement(
         _ = install_payload(installed.source, installed.profile, tmp_path, tmp_path)
     # Then the foreign replacement is preserved rather than overwritten by rollback.
     assert _snapshot(installed.target) == (("user-owned.txt", SENTINEL),)
-    assert list(installed.target.parent.iterdir()) == [installed.target]
+    recoveries = [path for path in installed.target.parent.iterdir() if path != installed.target]
+    assert len(recoveries) == 1
+    backup = recoveries[0] / "previous"
+    assert _snapshot(backup) == before
+    assert list(recoveries[0].iterdir()) == [backup]
